"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.serializeRouteOutput = serializeRouteOutput;
exports.deserializeRouteOutput = deserializeRouteOutput;
exports.serializeRouteParams = serializeRouteParams;
exports.serializeQuoteParams = serializeQuoteParams;
const types_1 = require("./types");
const anchor_1 = require("@coral-xyz/anchor");
const limo_sdk_1 = require("@kamino-finance/limo-sdk");
const consts_1 = require("../consts");
const kit_1 = require("@solana/kit");
const utils_1 = require("../utils");
const web3_js_1 = require("@solana/web3.js");
// Serialize VersionedTransaction to base64 string
function serializeVersionedTransaction(tx, lookupTableAccounts) {
    const versionedTx = (0, utils_1.toLegacyVersionedTransaction)(tx, lookupTableAccounts);
    const serializedTx = versionedTx.serialize();
    return Buffer.from(serializedTx).toString('base64');
}
// Deserialize VersionedTransaction from base64 string
function deserializeVersionedTransaction(base64Tx, lookupTableAccounts) {
    if (!base64Tx) {
        return undefined;
    }
    const txBuffer = Buffer.from(base64Tx, 'base64');
    return (0, utils_1.fromLegacyVersionedTransaction)(web3_js_1.VersionedTransaction.deserialize(txBuffer), lookupTableAccounts);
}
// Serialize TransactionInstruction[] to plain object array
function serializeTransactionInstructions(instructions) {
    return !instructions
        ? []
        : (0, utils_1.toLegacyInstructions)(...instructions).map((ix) => ({
            programId: ix.programId.toBase58(),
            data: ix.data.toString('base64'),
            keys: ix.keys.map((key) => ({
                pubkey: key.pubkey.toBase58(),
                isSigner: key.isSigner,
                isWritable: key.isWritable,
            })),
        }));
}
// Deserialize plain object array back to TransactionInstruction[]
function deserializeTransactionInstructions(instructions) {
    return !instructions
        ? []
        : instructions.map((ix) => {
            const { programId, data, keys } = ix;
            const decodedProgramId = new web3_js_1.PublicKey(programId);
            const decodedData = Buffer.from(data, 'base64');
            const decodedKeys = keys.map((key) => ({
                pubkey: new web3_js_1.PublicKey(key.pubkey),
                isSigner: key.isSigner,
                isWritable: key.isWritable,
            }));
            return (0, utils_1.fromLegacyInstruction)(new web3_js_1.TransactionInstruction({
                programId: decodedProgramId,
                data: decodedData,
                keys: decodedKeys,
            }));
        });
}
// Function to serialize AddressLookupTableState and AddressLookupTableAccountArgs
function serializeAddressLookupTableResponse(addressLookupTableAccounts) {
    return (0, utils_1.toLegacyLookupTables)(...addressLookupTableAccounts).map((lutAccount) => {
        return {
            key: lutAccount.key.toBase58(), // Convert PublicKey to base58 string
            state: {
                deactivationSlot: lutAccount.state.deactivationSlot.toString(), // Convert bigint to string
                lastExtendedSlot: lutAccount.state.lastExtendedSlot,
                lastExtendedSlotStartIndex: lutAccount.state.lastExtendedSlotStartIndex,
                authority: lutAccount.state.authority ? lutAccount.state.authority.toBase58() : undefined, // Convert PublicKey to base58 string
                addresses: lutAccount.state.addresses.map((address) => address.toBase58()), // Convert each PublicKey in the array to base58 string
            },
        };
    });
}
// Deserialize the AddressLookupTableState and AddressLookupTableAccountArgs response
function deserializeAddressLookupTableResponse(addressLookupTableAccountsSerialized) {
    return addressLookupTableAccountsSerialized.map((lutAccount) => {
        // Deserialize the state
        const deserializedState = {
            deactivationSlot: BigInt(lutAccount.state.deactivationSlot), // Convert deactivationSlot to bigint
            lastExtendedSlot: lutAccount.state.lastExtendedSlot,
            lastExtendedSlotStartIndex: lutAccount.state.lastExtendedSlotStartIndex,
            authority: lutAccount.state.authority ? new web3_js_1.PublicKey(lutAccount.state.authority) : undefined, // Convert PublicKey if present
            addresses: lutAccount.state.addresses.map((address) => new web3_js_1.PublicKey(address)), // Convert each address to PublicKey
        };
        // Deserialize the key as PublicKey
        const deserializedKey = new web3_js_1.PublicKey(lutAccount.key);
        // Return the deserialized AddressLookupTableAccountArgs
        return (0, utils_1.fromLegacyLookupTable)(new web3_js_1.AddressLookupTableAccount({
            key: deserializedKey,
            state: deserializedState,
        }));
    });
}
function serializeRouteOutput(routeOutput) {
    const baseOutput = {
        ixsRouterBs58: serializeTransactionInstructions(routeOutput.ixsRouter),
        amountsExactIn: {
            amountIn: routeOutput.amountsExactIn.amountIn.toString(),
            amountOutGuaranteed: routeOutput.amountsExactIn.amountOutGuaranteed.toString(),
            amountOut: routeOutput.amountsExactIn.amountOut.toString(),
            amountOutSimulated: routeOutput.amountsExactIn.amountOutSimulated
                ? routeOutput.amountsExactIn.amountOutSimulated.toString()
                : undefined,
        },
        amountsExactOut: {
            amountOut: routeOutput.amountsExactOut.amountOut.toString(),
            amountInGuaranteed: routeOutput.amountsExactOut.amountInGuaranteed.toString(),
            amountIn: routeOutput.amountsExactOut.amountIn.toString(),
            amountInSimulated: routeOutput.amountsExactOut.amountInSimulated
                ? routeOutput.amountsExactOut.amountInSimulated.toString()
                : undefined,
        },
        swapType: routeOutput.swapType,
        responseTimeGetQuoteMs: routeOutput.responseTimeGetQuoteMs,
        responseTimeSwapIxsMs: routeOutput.responseTimeSwapIxsMs,
        routerType: routeOutput.routerType,
        priceImpactBps: routeOutput.priceImpactBps,
        simulatedPriceImpactBps: routeOutput.simulatedPriceImpactBps,
        guaranteedPriceImpactBps: routeOutput.guaranteedPriceImpactBps,
        expiryTime: routeOutput.expiryTime,
        perReferenceId: routeOutput.perReferenceId,
        birdeyeTokenInPriceInSol: routeOutput.birdeyeTokenInPriceInSol,
        birdeyeTokenOutPriceInSol: routeOutput.birdeyeTokenOutPriceInSol,
        inputMintProgramOwner: routeOutput.inputMintProgramOwner ? routeOutput.inputMintProgramOwner : undefined,
        outputMintProgramOwner: routeOutput.outputMintProgramOwner ? routeOutput.outputMintProgramOwner : undefined,
        inputTokenDecimals: routeOutput.inputTokenDecimals,
        outputTokenDecimals: routeOutput.outputTokenDecimals,
        jupRequestId: routeOutput.jupRequestId,
        cloverDexType: routeOutput.cloverDexType,
        skipLimoLogsForRoute: routeOutput.skipLimoLogsForRoute ? 'true' : 'false',
        simulationResult: routeOutput.simulationResult
            ? serializeRouteSimulationResult(routeOutput.simulationResult)
            : undefined,
    };
    // Handle the discriminated union based on whether transaction exists
    if (routeOutput.transaction) {
        return {
            ...baseOutput,
            transactionBs58: serializeVersionedTransaction(routeOutput.transaction, routeOutput.lookupTableAccounts),
            lookupTableAccountsBs58: routeOutput.lookupTableAccounts?.length
                ? serializeAddressLookupTableResponse(routeOutput.lookupTableAccounts)
                : [],
        };
    }
    else {
        return {
            ...baseOutput,
            lookupTableAccountsBs58: routeOutput.lookupTableAccounts?.length
                ? serializeAddressLookupTableResponse(routeOutput.lookupTableAccounts)
                : undefined,
        };
    }
}
function serializeRouteSimulationResult(simulationResult) {
    return {
        simulationTimestamp: simulationResult.simulationTimestamp,
        errorReason: simulationResult.errorReason,
        logs: simulationResult.logs,
        success: simulationResult.success,
    };
}
function deserializeRouteSimulationResult(simulationResult) {
    return {
        simulationTimestamp: simulationResult.simulationTimestamp,
        errorReason: simulationResult.errorReason
            ? deserializeSimulationErrorReason(simulationResult.errorReason)
            : undefined,
        logs: simulationResult.logs,
        success: simulationResult.success,
    };
}
function deserializeSimulationErrorReason(errorReason) {
    const reason = errorReason;
    switch (reason) {
        case types_1.SimulationErrorReason.SlippageExceeded:
            return types_1.SimulationErrorReason.SlippageExceeded;
        case types_1.SimulationErrorReason.Unknown:
            return types_1.SimulationErrorReason.Unknown;
        case types_1.SimulationErrorReason.TooManyAccountLocks:
            return types_1.SimulationErrorReason.TooManyAccountLocks;
        default: {
            // Compile-time check: if a new enum member is added and not handled, this will error
            const exhaustiveCheck = reason;
            throw new Error(`Unknown SimulationErrorReason ${errorReason}`);
        }
    }
}
async function deserializeRouteOutput(routeOutput) {
    if (!(0, consts_1.isValidRouterType)(routeOutput.routerType)) {
        return undefined;
    }
    const lookupTableAccounts = deserializeAddressLookupTableResponse(routeOutput.lookupTableAccountsBs58 || []);
    return {
        ixsRouter: deserializeTransactionInstructions(routeOutput.ixsRouterBs58),
        transaction: deserializeVersionedTransaction(routeOutput.transactionBs58, lookupTableAccounts),
        amountsExactIn: {
            amountIn: new anchor_1.BN(routeOutput.amountsExactIn.amountIn),
            amountOutGuaranteed: new anchor_1.BN(routeOutput.amountsExactIn.amountOutGuaranteed),
            amountOut: new anchor_1.BN(routeOutput.amountsExactIn.amountOut),
            amountOutSimulated: routeOutput.amountsExactIn.amountOutSimulated
                ? new anchor_1.BN(routeOutput.amountsExactIn.amountOutSimulated)
                : undefined,
        },
        amountsExactOut: {
            amountOut: new anchor_1.BN(routeOutput.amountsExactOut.amountOut),
            amountInGuaranteed: new anchor_1.BN(routeOutput.amountsExactOut.amountInGuaranteed),
            amountIn: new anchor_1.BN(routeOutput.amountsExactOut.amountIn),
            amountInSimulated: routeOutput.amountsExactOut.amountInSimulated
                ? new anchor_1.BN(routeOutput.amountsExactOut.amountInSimulated)
                : undefined,
        },
        swapType: routeOutput.swapType,
        responseTimeGetQuoteMs: routeOutput.responseTimeGetQuoteMs,
        responseTimeSwapIxsMs: routeOutput.responseTimeSwapIxsMs,
        routerType: routeOutput.routerType,
        lookupTableAccounts: lookupTableAccounts,
        expiryTime: routeOutput.expiryTime,
        perReferenceId: routeOutput.perReferenceId,
        priceImpactBps: routeOutput.priceImpactBps,
        simulatedPriceImpactBps: routeOutput.simulatedPriceImpactBps,
        guaranteedPriceImpactBps: routeOutput.guaranteedPriceImpactBps,
        birdeyeTokenInPriceInSol: routeOutput.birdeyeTokenInPriceInSol,
        birdeyeTokenOutPriceInSol: routeOutput.birdeyeTokenOutPriceInSol,
        inputMintProgramOwner: routeOutput.inputMintProgramOwner ? (0, kit_1.address)(routeOutput.inputMintProgramOwner) : undefined,
        outputMintProgramOwner: routeOutput.outputMintProgramOwner
            ? (0, kit_1.address)(routeOutput.outputMintProgramOwner)
            : undefined,
        inputTokenDecimals: routeOutput.inputTokenDecimals,
        outputTokenDecimals: routeOutput.outputTokenDecimals,
        jupRequestId: routeOutput.jupRequestId,
        cloverDexType: routeOutput.cloverDexType,
        skipLimoLogsForRoute: routeOutput.skipLimoLogsForRoute === 'true', // Deserialize string to boolean
        simulationResult: routeOutput.simulationResult
            ? deserializeRouteSimulationResult(routeOutput.simulationResult)
            : undefined,
    };
}
function serializeRouteParams(routeParams) {
    const res = {
        tokenIn: routeParams.tokenIn,
        tokenOut: routeParams.tokenOut,
        amount: routeParams.amount.toString(),
        swapType: routeParams.swapType,
        executor: routeParams.executor,
        referrerPda: routeParams.referrerPda ? routeParams.referrerPda : limo_sdk_1.limoId,
        maxSlippageBps: routeParams.maxSlippageBps,
        includeSetupIxs: routeParams.includeSetupIxs !== undefined && !routeParams.includeSetupIxs ? 'false' : 'true',
        wrapAndUnwrapSol: routeParams.wrapAndUnwrapSol !== undefined && !routeParams.wrapAndUnwrapSol ? 'false' : 'true',
        routerTypes: routeParams.routerTypes,
        includeLimoLogs: routeParams.includeLimoLogs !== undefined && !routeParams.includeLimoLogs ? 'false' : 'true',
        includeRfq: routeParams.includeRfq !== undefined && !routeParams.includeRfq ? 'false' : 'true',
        timeoutMs: routeParams.timeoutMs,
        atLeastOneNoMoreThanTimeoutMS: routeParams.atLeastOneNoMoreThanTimeoutMS,
        withSimulation: routeParams.withSimulation !== undefined && !routeParams.withSimulation ? 'false' : 'true',
        filterFailedSimulations: routeParams.filterFailedSimulations !== undefined && routeParams.filterFailedSimulations ? 'true' : 'false',
        destinationTokenAccount: routeParams.destinationTokenAccount ? routeParams.destinationTokenAccount : undefined,
        preferredMaxAccounts: routeParams.preferredMaxAccounts,
        requestPriceImpact: routeParams.requestPriceImpact ? 'true' : 'false',
        perMinimumQuoteLifetimeSeconds: routeParams.perMinimumQuoteLifetimeSeconds,
        assertSwapBalances: routeParams.assertSwapBalances ? 'true' : 'false',
        overrideAssertMaxInputAmountChange: routeParams.overrideAssertMaxInputAmountChange
            ? routeParams.overrideAssertMaxInputAmountChange.toString()
            : undefined,
        overrideAssertMinOutputAmountChange: routeParams.overrideAssertMinOutputAmountChange
            ? routeParams.overrideAssertMinOutputAmountChange.toString()
            : undefined,
        simulateWithMockInputAmount: routeParams.simulateWithMockInputAmount ? 'true' : 'false', // Serialize boolean as string
    };
    Object.keys(routeParams).forEach((key) => {
        if (!Object.hasOwn(res, key)) {
            throw new Error('serializeRouteParams: not all keys of routeParams have been serialized missing key: ' + key);
        }
    });
    return res;
}
function serializeQuoteParams(quoteParams) {
    const res = {
        tokenIn: quoteParams.tokenIn,
        tokenOut: quoteParams.tokenOut,
        amount: quoteParams.amount.toString(),
        swapType: quoteParams.swapType,
        maxSlippageBps: quoteParams.maxSlippageBps,
        routerTypes: quoteParams.routerTypes,
        includeRfq: quoteParams.includeRfq !== undefined && !quoteParams.includeRfq ? 'false' : 'true',
        timeoutMs: quoteParams.timeoutMs,
        atLeastOneNoMoreThanTimeoutMS: quoteParams.atLeastOneNoMoreThanTimeoutMS,
        preferredMaxAccounts: quoteParams.preferredMaxAccounts,
        requestPriceImpact: quoteParams.requestPriceImpact,
        executor: quoteParams.executor, // Always undefined for quotes
    };
    Object.keys(quoteParams).forEach((key) => {
        if (!Object.hasOwn(res, key)) {
            throw new Error('serializeQuoteParams: not all keys of quoteParams have been serialized missing key: ' + key);
        }
    });
    return res;
}
//# sourceMappingURL=serialization.js.map