"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.NoRouterContext = exports.RouterContext = void 0;
exports.loadRouterContext = loadRouterContext;
const types_1 = require("./types");
const limo_sdk_1 = require("@kamino-finance/limo-sdk");
const token_2022_1 = require("@solana-program/token-2022");
class RouterContext {
    inMintInfo;
    outMintInfo;
    constructor(inMintInfo, outMintInfo) {
        this.inMintInfo = inMintInfo;
        this.outMintInfo = outMintInfo;
    }
}
exports.RouterContext = RouterContext;
class NoRouterContext {
    get inMintInfo() {
        throw new Error(`NoRouterContext: inMintInfo is not available`);
    }
    get outMintInfo() {
        throw new Error(`NoRouterContext: outMintInfo is not available`);
    }
}
exports.NoRouterContext = NoRouterContext;
async function loadRouterContext(connection, fromToken, toToken) {
    let fromTokenInfo;
    let toTokenInfo;
    if (fromToken === limo_sdk_1.WRAPPED_SOL_MINT) {
        fromTokenInfo = types_1.SOL_MINT_INFO;
    }
    if (toToken === limo_sdk_1.WRAPPED_SOL_MINT) {
        toTokenInfo = types_1.SOL_MINT_INFO;
    }
    if (fromTokenInfo && toTokenInfo) {
        return new RouterContext(fromTokenInfo, toTokenInfo);
    }
    else if (toTokenInfo) {
        const fromTokenInfo = await fetchSingleMintInfo(connection, fromToken);
        return new RouterContext(fromTokenInfo, toTokenInfo);
    }
    else if (fromTokenInfo) {
        const toTokenInfo = await fetchSingleMintInfo(connection, toToken);
        return new RouterContext(fromTokenInfo, toTokenInfo);
    }
    const mintAccounts = await (0, token_2022_1.fetchAllMaybeMint)(connection, [fromToken, toToken]);
    const fromMintAcc = mintAccounts[0];
    const toMintAcc = mintAccounts[1];
    if (!fromMintAcc.exists) {
        throw new Error(`Failed to fetch mint account info ${fromToken}`);
    }
    if (!toMintAcc.exists) {
        throw new Error(`Failed to fetch mint account info ${toToken}`);
    }
    return new RouterContext({ tokenProgramId: fromMintAcc.programAddress, decimals: fromMintAcc.data.decimals }, { tokenProgramId: toMintAcc.programAddress, decimals: toMintAcc.data.decimals });
}
async function fetchSingleMintInfo(connection, mint) {
    const mintInfo = await (0, token_2022_1.fetchMaybeMint)(connection, mint);
    if (!mintInfo.exists) {
        throw new Error(`Failed to fetch mint account info ${mint}`);
    }
    return {
        tokenProgramId: mintInfo.programAddress,
        decimals: mintInfo.data.decimals,
    };
}
//# sourceMappingURL=RouterContext.js.map