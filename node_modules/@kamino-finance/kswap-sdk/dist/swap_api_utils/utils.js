"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.wrapAndUnwrapSolIxs = wrapAndUnwrapSolIxs;
exports.unwrapSolIx = unwrapSolIx;
exports.wrapSolIxs = wrapSolIxs;
exports.getDepositWsolIxs = getDepositWsolIxs;
exports.getInitOutputAtaIxs = getInitOutputAtaIxs;
exports.isRequestedRouterType = isRequestedRouterType;
exports.filterArrayRouterTypes = filterArrayRouterTypes;
exports.safeBNToNumber = safeBNToNumber;
const limo_sdk_1 = require("@kamino-finance/limo-sdk");
const utils_1 = require("../utils");
const consts_1 = require("../consts");
const decimal_js_1 = __importDefault(require("decimal.js"));
const kit_1 = require("@solana/kit");
const token_1 = require("@solana-program/token");
const system_1 = require("@solana-program/system");
const token_2022_1 = require("@solana-program/token-2022");
async function wrapAndUnwrapSolIxs(params) {
    let wrapIxs = [];
    let unwrapIxs = [];
    if (params.tokenIn === limo_sdk_1.WRAPPED_SOL_MINT && params.wrapAndUnwrapSol) {
        const [wrapIxsResult, unwrapIxResult] = await Promise.all([
            wrapSolIxs(params, undefined, true),
            unwrapSolIx(params, undefined),
        ]);
        wrapIxs = wrapIxsResult;
        unwrapIxs = [unwrapIxResult];
    }
    if (params.tokenOut === limo_sdk_1.WRAPPED_SOL_MINT && params.wrapAndUnwrapSol) {
        const [wrapIxsResult, unwrapIxResult] = await Promise.all([
            wrapSolIxs(params, undefined, false),
            unwrapSolIx(params, params.destinationTokenAccount),
        ]);
        wrapIxs = wrapIxsResult;
        unwrapIxs = [unwrapIxResult];
    }
    return { wrapIxs, unwrapIxs };
}
async function unwrapSolIx(params, wsolTa) {
    let wsolAta = wsolTa;
    if (!wsolAta) {
        const [associatedTokenAddress] = await (0, token_2022_1.findAssociatedTokenPda)({
            mint: limo_sdk_1.WRAPPED_SOL_MINT,
            owner: params.executor,
            tokenProgram: token_1.TOKEN_PROGRAM_ADDRESS,
        });
        wsolAta = associatedTokenAddress;
    }
    const unwrapSolIx = (0, token_2022_1.getCloseAccountInstruction)({
        account: wsolAta,
        destination: params.executor,
        owner: params.executor,
        multiSigners: [],
    }, { programAddress: token_1.TOKEN_PROGRAM_ADDRESS });
    return unwrapSolIx;
}
async function wrapSolIxs(params, wsolTa, withDeposit) {
    const ixs = [];
    let ata;
    let createAtaIxs;
    if (wsolTa) {
        ata = wsolTa;
        createAtaIxs = [];
    }
    else {
        const ataInfo = await (0, utils_1.createAtaIdempotent)(params.executor, (0, kit_1.createNoopSigner)(params.executor), limo_sdk_1.WRAPPED_SOL_MINT, token_1.TOKEN_PROGRAM_ADDRESS);
        ata = ataInfo.ata;
        createAtaIxs = [ataInfo.createAtaIx];
    }
    ixs.push(...createAtaIxs);
    if (withDeposit && params.amount) {
        ixs.push(...getDepositWsolIxs((0, kit_1.createNoopSigner)(params.executor), ata, params.amount));
    }
    return ixs;
}
/**
 * Get the deposit WSOL instructions
 * @param owner - the owner address
 * @param ata - the ata address
 * @param amountLamports - the amount in lamports
 * @returns the transaction instructions
 */
function getDepositWsolIxs(owner, ata, amountLamports) {
    const ixns = [];
    // Transfer to WSOL ata
    ixns.push((0, system_1.getTransferSolInstruction)({
        source: owner,
        destination: ata,
        amount: BigInt(amountLamports.toString()),
    }));
    // Sync wrapped SOL
    ixns.push((0, token_2022_1.getSyncNativeInstruction)({ account: ata }, {
        programAddress: token_1.TOKEN_PROGRAM_ADDRESS,
    }));
    return ixns;
}
async function getInitOutputAtaIxs(params, ctx) {
    if (params.destinationTokenAccount !== undefined ||
        params.includeSetupIxs === false ||
        (params.tokenOut === limo_sdk_1.WRAPPED_SOL_MINT && params.wrapAndUnwrapSol)) {
        // we assume that the wrap and unwrap ixs handle the WSOL ata creation
        return [];
    }
    else {
        const [_, initOutAtaIx] = await (0, utils_1.createAssociatedTokenAccountIdempotentInstruction)(params.executor, params.tokenOut, (0, kit_1.createNoopSigner)(params.executor), ctx.outMintInfo.tokenProgramId);
        return [initOutAtaIx];
    }
}
function isRequestedRouterType(requestedFilterArray, routerType) {
    return requestedFilterArray.length == 0 || requestedFilterArray.includes(routerType);
}
function filterArrayRouterTypes(routerTypes, includeRfq) {
    // Handle the case when no router types are specified - use all routers
    const filterArray = !routerTypes || routerTypes.length === 0 ? [...consts_1.SUPPORTED_ROUTER_TYPES] : routerTypes;
    if (!includeRfq) {
        // Remove RFQ routers from the filter array
        return filterArray.filter((routerType) => !consts_1.RFQ_ROUTERS.includes(routerType));
    }
    // Filter out invalid router types (should already be validated at controller level)
    return filterArray;
}
function safeBNToNumber(value) {
    return new decimal_js_1.default(value.toString()).toNumber();
}
//# sourceMappingURL=utils.js.map