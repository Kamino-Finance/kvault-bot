"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.TransactionError = exports.instructionEquals = exports.getLookupTableAccountsFromKeys = exports.INVALID_BUT_SUFFICIENT_FOR_COMPILATION_BLOCKHASH = exports.MAX_LOCKED_ACCOUNTS = void 0;
exports.base64EncodeTx = base64EncodeTx;
exports.sendTx = sendTx;
exports.removeComputeBudgetIxns = removeComputeBudgetIxns;
exports.sendAndConfirmTx = sendAndConfirmTx;
exports.sendAndConfirmTransactionV0Impl = sendAndConfirmTransactionV0Impl;
exports.confirmTx = confirmTx;
exports.forceGetConfirmedTx = forceGetConfirmedTx;
exports.getSimulationComputeUnits = getSimulationComputeUnits;
exports.maxLockedAccounts = maxLockedAccounts;
exports.uniqueAccounts = uniqueAccounts;
exports.arrayDeepEquals = arrayDeepEquals;
exports.createAddExtraComputeUnitsTransaction = createAddExtraComputeUnitsTransaction;
exports.getComputeUnitPrice = getComputeUnitPrice;
exports.isComputeBudgetComputeUnitLimit = isComputeBudgetComputeUnitLimit;
exports.isComputeBudgetComputeUnitPrice = isComputeBudgetComputeUnitPrice;
exports.overwriteComputeBudget = overwriteComputeBudget;
exports.getRecentAverageFees = getRecentAverageFees;
exports.getAverageFeesPerCUForPeriodLamports = getAverageFeesPerCUForPeriodLamports;
exports.getAverageFees = getAverageFees;
exports.getRpcRecentFeesOfPercentile = getRpcRecentFeesOfPercentile;
const web3_js_1 = require("@solana/web3.js");
const decimal_js_1 = __importDefault(require("decimal.js"));
const uuid_1 = require("uuid");
const utils_1 = require("./utils");
const consts_1 = require("../consts");
const compute_budget_1 = require("@solana-program/compute-budget");
const kit_1 = require("@solana/kit");
const address_lookup_table_1 = require("@solana-program/address-lookup-table");
exports.MAX_LOCKED_ACCOUNTS = 64;
exports.INVALID_BUT_SUFFICIENT_FOR_COMPILATION_BLOCKHASH = {
    blockhash: '11111111111111111111111111111111',
    lastValidBlockHeight: 0n,
    slot: 0n,
};
function base64EncodeTx(cluster, payer, instructions, lookupTables = undefined) {
    const luts = {};
    (lookupTables || []).forEach((lut) => {
        luts[lut.address] = lut.data.addresses;
    });
    const transactionMessage = (0, utils_1.buildTransactionMessage)(instructions, payer, exports.INVALID_BUT_SUFFICIENT_FOR_COMPILATION_BLOCKHASH, lookupTables);
    const compiled = (0, kit_1.compileTransactionMessage)(transactionMessage);
    const encodedMessageBytes = (0, kit_1.getCompiledTransactionMessageEncoder)().encode(compiled);
    const encodedTxMessage = Buffer.from(encodedMessageBytes).toString('base64');
    const clusterString = cluster === 'localnet' ? '?cluster=custom&customUrl=http://localhost:8899' : `?cluster=${cluster.toString()}`;
    const simulationUrl = `https://explorer.solana.com/tx/inspector${clusterString}&message=${encodeURIComponent(encodedTxMessage)}&signatures=${encodeURIComponent(`[${payer}]`)}`;
    return { simulationUrl };
}
async function sendTx(rpc, tx, sig, blockhash, withDescription = '', logger = console.log) {
    const serialized = (0, kit_1.getBase64EncodedWireTransaction)(tx);
    const link = `https://solscan.io/tx/${sig}`;
    logger(`${withDescription} ${link}`);
    try {
        await rpc
            .sendTransaction(serialized, {
            encoding: 'base64',
            preflightCommitment: 'confirmed',
            maxRetries: 0n,
            skipPreflight: true,
            minContextSlot: blockhash.slot,
        })
            .send();
        return sig;
    }
    catch (e) {
        logger(`${withDescription} ${sig}, RPC rejected transaction  with the following error:\n ${e}`);
        const errString = e.toString();
        if (errString.includes('failed')) {
            const sig = e.toString().split(' failed ')[0].split('Transaction ')[1];
            if (!sig) {
                throw e;
            }
            const failedTx = await forceGetConfirmedTx(rpc, sig);
            if (!failedTx) {
                throw new TransactionError(e.message, sig, undefined, e);
            }
            else {
                let logs = undefined;
                if (failedTx.meta?.logMessages) {
                    logs = [...failedTx.meta.logMessages];
                }
                throw new TransactionError(e.message, sig, logs, e);
            }
        }
        else {
            throw e;
        }
    }
}
function removeComputeBudgetIxns(ixns) {
    return ixns.filter(({ programAddress }) => programAddress !== compute_budget_1.COMPUTE_BUDGET_PROGRAM_ADDRESS);
}
async function sendAndConfirmTx({ rpc, wsRpc, }, tx, slot) {
    await (0, kit_1.sendAndConfirmTransactionFactory)({ rpc, rpcSubscriptions: wsRpc })({ ...tx, lifetimeConstraint: { lastValidBlockHeight: slot } }, {
        commitment: 'confirmed',
        preflightCommitment: 'confirmed',
        maxRetries: 0n,
        skipPreflight: true,
        minContextSlot: slot,
    });
}
async function sendAndConfirmTransactionV0Impl(rpc, sig, tx, slot, withDescription = '', logger = console.log) {
    try {
        const link = `https://solscan.io/tx/${sig}`;
        logger(`${withDescription} ${link}`);
        await sendAndConfirmTx(rpc, tx, slot);
    }
    catch (e) {
        const failedTx = await forceGetConfirmedTx(rpc.rpc, sig);
        if (!failedTx) {
            throw new TransactionError(`Failed to send transaction: ${withDescription} ${sig}`, sig, undefined, e);
        }
        else {
            let logs = undefined;
            if (failedTx.meta?.logMessages) {
                logs = [...failedTx.meta.logMessages];
            }
            throw new TransactionError(`Failed to send transaction: ${withDescription} ${sig}`, sig, logs, e);
        }
    }
}
async function confirmTx(connection, txHash) {
    const sig = (0, kit_1.signature)(txHash);
    return connection
        .getTransaction(sig, {
        commitment: 'confirmed',
        encoding: 'json',
        maxSupportedTransactionVersion: 0,
    })
        .send();
}
async function forceGetConfirmedTx(rpc, sig) {
    console.log(`forceGetConfirmedTx: ${sig}`);
    const endTime = Date.now() + 5000;
    let lastErr = null;
    let failedTx = null;
    while (true) {
        try {
            failedTx = await rpc
                .getTransaction(sig, {
                commitment: 'confirmed',
                maxSupportedTransactionVersion: 0,
                encoding: 'json',
            })
                .send();
            if (failedTx) {
                return failedTx;
            }
        }
        catch (e) {
            lastErr = e;
        }
        if (Date.now() > endTime) {
            if (lastErr) {
                throw lastErr;
            }
            else {
                return failedTx;
            }
        }
    }
}
async function getSimulationComputeUnits(c, instructions, payer, lookupTables, withDescription = '', logger = console.log) {
    // Set high to ensure it doesn't fail
    const testInstructions = overwriteComputeBudget(instructions, 2_000_000);
    const transactionMessage = (0, utils_1.buildTransactionMessage)(testInstructions, payer, exports.INVALID_BUT_SUFFICIENT_FOR_COMPILATION_BLOCKHASH, lookupTables);
    const testTransaction = await (0, kit_1.signTransactionMessageWithSigners)(transactionMessage);
    const encodedTransaction = (0, kit_1.getBase64EncodedWireTransaction)(testTransaction);
    try {
        let rpcResponse = await c
            .simulateTransaction(encodedTransaction, {
            encoding: 'base64',
            replaceRecentBlockhash: true,
            sigVerify: false,
        })
            .send();
        if (rpcResponse.value.err) {
            logger(`${withDescription} Error response from simulation: ${JSON.stringify(rpcResponse.value.err)}, logs: ${rpcResponse.value.logs}`);
            return null;
        }
        // getErrorFromRPCResponse(rpcResponse);
        return Number(rpcResponse.value.unitsConsumed) || null;
    }
    catch (e) {
        logger(`${withDescription} Error simulating transaction: ${e}`);
        return null;
    }
}
const getLookupTableAccountsFromKeys = async (connection, keys) => {
    return await (0, address_lookup_table_1.fetchAllAddressLookupTable)(connection, keys);
};
exports.getLookupTableAccountsFromKeys = getLookupTableAccountsFromKeys;
function maxLockedAccounts(count) {
    return exports.MAX_LOCKED_ACCOUNTS - count;
}
function isAddressLookupTableAccountArray(items) {
    return (Array.isArray(items) &&
        items.length > 0 &&
        items.every((item) => item &&
            typeof item === 'object' &&
            'address' in item &&
            'data' in item &&
            item.data &&
            'addresses' in item.data));
}
function uniqueAccounts(ixs, addressLookupTables = []) {
    let luts;
    if (isAddressLookupTableAccountArray(addressLookupTables)) {
        luts = addressLookupTables.map((lut) => lut.address);
    }
    else {
        luts = addressLookupTables.map((k) => k);
    }
    const uniqueAccounts = ixs
        .map((ix) => ix.accounts?.map((k) => k.address.toString()) ?? [])
        .flat()
        .concat(...luts);
    return new Set(uniqueAccounts);
}
const instructionEquals = (ix1, ix2) => ix1.programAddress === ix2.programAddress &&
    arrayDeepEquals(ix1.accounts, ix2.accounts, (a, b) => a.role === b.role && a.address === b.address) &&
    arrayDeepEquals(ix1.data ? Array.from(ix1.data) : undefined, ix2.data ? Array.from(ix2.data) : undefined, (a, b) => a === b);
exports.instructionEquals = instructionEquals;
function arrayDeepEquals(array1, array2, eq) {
    if (array1 === undefined && array2 === undefined) {
        return true;
    }
    if (array1 === undefined || array2 === undefined) {
        return false;
    }
    if (array1.length !== array2.length) {
        return false;
    }
    return array1.reduce((prev, current, index) => {
        const other = array2[index];
        if (other == null) {
            return false;
        }
        return prev && eq(current, other);
    }, true);
}
class TransactionError extends Error {
    sig;
    logs;
    err;
    constructor(message, sig, logs, err) {
        super(message);
        this.sig = sig;
        this.logs = logs;
        this.err = err;
    }
}
exports.TransactionError = TransactionError;
function createAddExtraComputeUnitsTransaction(units, feePerCULamports) {
    const ixns = [];
    ixns.push((0, compute_budget_1.getSetComputeUnitLimitInstruction)({ units }));
    if (feePerCULamports) {
        const { microLamports } = getComputeUnitPrice(units, feePerCULamports);
        ixns.push((0, compute_budget_1.getSetComputeUnitPriceInstruction)({ microLamports }));
    }
    return ixns;
}
function getComputeUnitPrice(units, feePerCULamports) {
    const multiplier = 1;
    const unitPrice = feePerCULamports
        .mul(10 ** 6)
        .div(units)
        .mul(multiplier);
    const value = unitPrice.floor().toString();
    console.debug(`Fee per CU lamports: ${feePerCULamports}, units: ${units}, compute unit price: ${value}`);
    return { microLamports: BigInt(value) };
}
function isComputeBudgetComputeUnitLimit(ix) {
    return ix === compute_budget_1.ComputeBudgetInstruction.SetComputeUnitLimit;
}
function isComputeBudgetComputeUnitPrice(ix) {
    return ix === compute_budget_1.ComputeBudgetInstruction.SetComputeUnitPrice;
}
function eliminateConsecutive(arr, comparator) {
    if (!arr.length)
        return [];
    const result = [arr[0]];
    for (let i = 1; i < arr.length; i += 1) {
        if (comparator) {
            if (!comparator(arr[i], arr[i - 1])) {
                result.push(arr[i]);
            }
        }
        else if (arr[i] !== arr[i - 1]) {
            result.push(arr[i]);
        }
    }
    return result;
}
function overwriteComputeBudget(ixs, units) {
    const ixns = ixs.filter((ix) => ix.programAddress !== compute_budget_1.COMPUTE_BUDGET_PROGRAM_ADDRESS ||
        (ix.data && !isComputeBudgetComputeUnitLimit((0, compute_budget_1.identifyComputeBudgetInstruction)(ix.data))));
    ixns.unshift((0, compute_budget_1.getSetComputeUnitLimitInstruction)({ units }));
    return ixns;
}
const SOLANA_COMPASS_API_URL = (0, utils_1.getEnvOrDefault)('SOLANA_COMPASS_API_URL', 'https://solanacompass.com/api');
async function getRecentAverageFees(cluster) {
    try {
        if (cluster === 'mainnet-beta') {
            return new decimal_js_1.default(await getAverageFeesPerCUForPeriodLamports(1));
        }
        return undefined;
    }
    catch (e) {
        console.warn('Failed to get average fees', e);
        return undefined;
    }
}
async function getAverageFeesPerCUForPeriodLamports(periodMins) {
    const averageFees = await getAverageFees();
    return averageFees[periodMins].avg;
}
async function getAverageFees() {
    const res = await fetch(`${SOLANA_COMPASS_API_URL}/fees`);
    return (await res.json());
}
/**
 * Supports the [Triton improved recent priority fee api](https://docs.triton.one/chains/solana/improved-priority-fees-api) which adds a percentile parameter to the getRecentPrioritizationFees rpc call.
 * @param c
 * @param rpcEndpoint
 * @param config
 */
async function getRpcRecentFeesOfPercentile(c, rpcEndpoint, config) {
    const percentileStr = `${config.percentile ? ` P[${config.percentile / 100}]` : ''}`;
    const accStr = config.addresses?.length || 'all global';
    console.log(`Fetching recent${percentileStr} priority fees from RPC for ${accStr} accounts`);
    const [res, slot] = await Promise.all([
        getRpcRecentPrioritizationFeesWithPercentile(config, rpcEndpoint),
        c.getSlot().send(),
    ]);
    const validSlot = Number(slot) - consts_1.SLOTS_PER_MINUTE;
    let filteredZeros = 0;
    let filteredOld = 0;
    let total = new decimal_js_1.default('0');
    const recentFees = res.result
        .filter((f) => {
        if (f.prioritizationFee <= 0) {
            filteredZeros++;
            return false;
        }
        if (f.slot < validSlot) {
            filteredOld++;
            return false;
        }
        total = total.add(f.prioritizationFee);
        return true;
    })
        .sort((a, b) => a.prioritizationFee - b.prioritizationFee);
    if (recentFees.length === 0) {
        console.log(`No valid 1 minute${percentileStr} priority fees returned from RPC for ${accStr} accounts, using 1 uLamports/CU. Filtered ${filteredZeros} zero fees and ${filteredOld} >1 minute fees`);
        return {
            min: new decimal_js_1.default('1'),
            max: new decimal_js_1.default('1'),
            average: new decimal_js_1.default('1'),
            median: new decimal_js_1.default('1'),
        };
    }
    if (recentFees.length === 1) {
        console.log(`Fetched 1 valid 1 minute${percentileStr} priority fee from RPC for ${accStr} accounts, fee: ${recentFees[0].prioritizationFee} uLamports/CU. Filtered ${filteredZeros} zero fees and ${filteredOld} >1 minute fees`);
        return {
            min: new decimal_js_1.default(recentFees[0].prioritizationFee),
            max: new decimal_js_1.default(recentFees[0].prioritizationFee),
            average: new decimal_js_1.default(recentFees[0].prioritizationFee),
            median: new decimal_js_1.default(recentFees[0].prioritizationFee),
        };
    }
    const min = new decimal_js_1.default(recentFees[0].prioritizationFee);
    const max = new decimal_js_1.default(recentFees[recentFees.length - 1].prioritizationFee);
    const average = total.div(recentFees.length);
    const median = new decimal_js_1.default(recentFees[Math.floor(recentFees.length / 2)].prioritizationFee);
    console.log(`Fetched ${recentFees.length} valid 1 minute${percentileStr} priority fees from RPC for ${accStr} accounts, median: ${median} uLamports/CU, average: ${average} uLamports/CU, min: ${min} uLamports/CU, max: ${max} uLamports/CU. Filtered ${filteredZeros} zero fees and ${filteredOld} >1 minute fees`);
    return {
        min,
        max,
        average,
        median,
    };
}
/**
 * Supports the [Triton improved recent priority fee api](https://docs.triton.one/chains/solana/improved-priority-fees-api) which adds a percentile parameter to the getRecentPrioritizationFees rpc call.
 * @param rpcEndpoint
 * @param config
 */
async function getRpcRecentPrioritizationFeesWithPercentile(config, rpcEndpoint) {
    const accounts = config?.addresses || [];
    const percentileConfig = config.percentile ? { percentile: config.percentile } : {};
    const response = await fetch(rpcEndpoint, {
        method: 'POST',
        headers: {
            Accept: 'application/json',
            'Content-Type': 'application/json',
            'Cache-Control': 'no-cache',
        },
        body: JSON.stringify({
            method: 'getRecentPrioritizationFees',
            jsonrpc: '2.0',
            params: [
                [...accounts],
                {
                    ...percentileConfig,
                },
            ],
            id: (0, uuid_1.v4)(),
        }),
    });
    if (!response.ok) {
        throw new Error(`Network response was not ok: ${response.statusText}`);
    }
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const unsafeRes = await response.json();
    if ('error' in unsafeRes) {
        throw new web3_js_1.SolanaJSONRPCError(unsafeRes.error, `Failed to get recent priority fees by accounts: ${config?.addresses || []}`);
    }
    return unsafeRes;
}
//# sourceMappingURL=sendTransaction.js.map