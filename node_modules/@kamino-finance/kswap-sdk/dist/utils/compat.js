"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.fromLegacyPublicKeys = fromLegacyPublicKeys;
exports.fromLegacyPublicKey = fromLegacyPublicKey;
exports.toLegacyPublicKeys = toLegacyPublicKeys;
exports.toLegacyPublicKey = toLegacyPublicKey;
exports.getLegacyKeypair = getLegacyKeypair;
exports.fromLegacyInstructions = fromLegacyInstructions;
exports.fromLegacyInstruction = fromLegacyInstruction;
exports.toLegacyInstructions = toLegacyInstructions;
exports.fromLegacyLookupTables = fromLegacyLookupTables;
exports.fromLegacyLookupTable = fromLegacyLookupTable;
exports.toLegacyLookupTables = toLegacyLookupTables;
exports.toLegacyOptionalAddress = toLegacyOptionalAddress;
exports.determineRole = determineRole;
exports.fromLegacyVersionedTransaction = fromLegacyVersionedTransaction;
exports.fromLegacyVersionedTransactionWithAddressLookupTable = fromLegacyVersionedTransactionWithAddressLookupTable;
exports.toLegacyVersionedTransaction = toLegacyVersionedTransaction;
exports.extractLutAddressesFromTransaction = extractLutAddressesFromTransaction;
const web3_js_1 = require("@solana/web3.js");
const kit_1 = require("@solana/kit");
const address_lookup_table_1 = require("@solana-program/address-lookup-table");
const compat_1 = require("@solana/compat");
function fromLegacyPublicKeys(legacy) {
    return legacy.map(fromLegacyPublicKey);
}
function fromLegacyPublicKey(legacy) {
    return (0, kit_1.address)(legacy.toString());
}
function toLegacyPublicKeys(addresses) {
    return addresses.map(toLegacyPublicKey);
}
function toLegacyPublicKey(address) {
    return new web3_js_1.PublicKey(address.toString());
}
function getLegacyKeypair(bytes) {
    return web3_js_1.Keypair.fromSecretKey(bytes);
}
function fromLegacyInstructions(...legacy) {
    return legacy.map(fromLegacyInstruction);
}
function fromLegacyInstruction(legacy) {
    const accounts = legacy.keys.map((key) => ({
        address: fromLegacyPublicKey(key.pubkey),
        role: determineRole(key.isSigner, key.isWritable),
    }));
    return {
        programAddress: fromLegacyPublicKey(legacy.programId),
        accounts,
        data: legacy.data,
    };
}
function toLegacyInstructions(...instructions) {
    return instructions.map(toLegacyInstruction);
}
function toLegacyInstruction(instruction) {
    const keys = (instruction.accounts || []).map((account) => ({
        pubkey: toLegacyPublicKey(account.address),
        isSigner: account.role === kit_1.AccountRole.WRITABLE_SIGNER || account.role === kit_1.AccountRole.READONLY_SIGNER,
        isWritable: account.role === kit_1.AccountRole.WRITABLE_SIGNER || account.role === kit_1.AccountRole.WRITABLE,
    }));
    return new web3_js_1.TransactionInstruction({
        keys,
        programId: toLegacyPublicKey(instruction.programAddress),
        data: instruction.data ? Buffer.from(instruction.data) : undefined,
    });
}
function fromLegacyLookupTables(...account) {
    return account.map(fromLegacyLookupTable);
}
function fromLegacyLookupTable(lut) {
    return {
        address: fromLegacyPublicKey(lut.key),
        programAddress: address_lookup_table_1.ADDRESS_LOOKUP_TABLE_PROGRAM_ADDRESS,
        executable: false,
        lamports: (0, kit_1.lamports)(0n),
        space: 0n,
        data: {
            padding: 0,
            lastExtendedSlotStartIndex: lut.state.lastExtendedSlotStartIndex,
            lastExtendedSlot: BigInt(lut.state.lastExtendedSlot),
            addresses: lut.state.addresses.map(fromLegacyPublicKey),
            authority: lut.state.authority ? (0, kit_1.some)(fromLegacyPublicKey(lut.state.authority)) : (0, kit_1.none)(),
            discriminator: 0,
            deactivationSlot: lut.state.deactivationSlot,
        },
    };
}
function toLegacyLookupTables(...account) {
    return account.map(toLegacyLookupTable);
}
function toLegacyLookupTable(account) {
    return new web3_js_1.AddressLookupTableAccount({
        key: toLegacyPublicKey(account.address),
        state: {
            lastExtendedSlotStartIndex: account.data.lastExtendedSlotStartIndex,
            lastExtendedSlot: Number(account.data.lastExtendedSlot),
            addresses: account.data.addresses.map(toLegacyPublicKey),
            authority: (0, kit_1.isSome)(account.data.authority)
                ? toLegacyPublicKey(account.data.authority.value)
                : undefined,
            deactivationSlot: account.data.deactivationSlot,
        },
    });
}
function toLegacyOptionalAddress(address, nullValue) {
    return address.equals(web3_js_1.PublicKey.default) ? nullValue : address;
}
function determineRole(isSigner, isWritable) {
    if (isSigner && isWritable)
        return kit_1.AccountRole.WRITABLE_SIGNER;
    if (isSigner)
        return kit_1.AccountRole.READONLY_SIGNER;
    if (isWritable)
        return kit_1.AccountRole.WRITABLE;
    return kit_1.AccountRole.READONLY;
}
function fromLegacyVersionedTransaction(transaction, lookupTableAccounts) {
    const hasALTs = lookupTableAccounts.length > 0;
    return hasALTs
        ? fromLegacyVersionedTransactionWithAddressLookupTable(transaction, toLegacyLookupTables(...lookupTableAccounts))
        : (0, compat_1.fromVersionedTransaction)(transaction);
}
function fromLegacyVersionedTransactionWithAddressLookupTable(transaction, addressLookupTableAccounts) {
    // Temporarily override getAccountKeys method
    const originalGetAccountKeys = transaction.message.getAccountKeys.bind(transaction.message);
    try {
        // Override with resolved version
        transaction.message.getAccountKeys = function (args) {
            return originalGetAccountKeys({ addressLookupTableAccounts });
        };
        const result = (0, compat_1.fromVersionedTransaction)(transaction);
        // Restore before returning
        transaction.message.getAccountKeys = originalGetAccountKeys;
        return result;
    }
    catch (error) {
        // Restore on error
        transaction.message.getAccountKeys = originalGetAccountKeys;
        throw error;
    }
}
function toLegacyVersionedTransaction(transaction, lookupTableAccounts) {
    try {
        // Deserialize the message bytes to get the VersionedMessage
        const versionedMessage = web3_js_1.VersionedMessage.deserialize(Buffer.from(transaction.messageBytes));
        // Create the versioned transaction from the message
        const versionedTransaction = new web3_js_1.VersionedTransaction(versionedMessage);
        const addressLookupTableAccounts = lookupTableAccounts.map(toLegacyLookupTable);
        let staticAccountKeys;
        let numRequiredSignatures;
        if (addressLookupTableAccounts.length > 0) {
            // Get account keys with resolved lookup tables
            const accountKeys = versionedMessage.getAccountKeys({ addressLookupTableAccounts });
            staticAccountKeys = accountKeys.staticAccountKeys;
            numRequiredSignatures = versionedMessage.header.numRequiredSignatures;
        }
        else {
            // No LUTs, use the simpler version
            const accountKeys = versionedMessage.getAccountKeys();
            staticAccountKeys = accountKeys.staticAccountKeys;
            numRequiredSignatures = versionedMessage.header.numRequiredSignatures;
        }
        // Create signatures array in the correct order (reverse of convertSignatures)
        const signaturesArray = [];
        for (let i = 0; i < numRequiredSignatures; i++) {
            const signerAddress = staticAccountKeys[i].toBase58();
            const signature = transaction.signatures[(0, kit_1.address)(signerAddress)];
            if (signature === null || signature === undefined) {
                // Convert null signatures back to all-zero signatures
                signaturesArray.push(new Uint8Array(64)); // all zeros
            }
            else {
                // Convert SignatureBytes back to Uint8Array
                signaturesArray.push(new Uint8Array(signature));
            }
        }
        // Validate signature count matches requirements
        if (signaturesArray.length !== numRequiredSignatures) {
            throw new Error(`Signature count mismatch: expected ${numRequiredSignatures}, got ${signaturesArray.length}`);
        }
        // Set the signatures on the versioned transaction
        versionedTransaction.signatures = signaturesArray;
        return versionedTransaction;
    }
    catch (error) {
        throw error;
    }
}
function extractLutAddressesFromTransaction(transaction) {
    try {
        const dMessage = (0, kit_1.getCompiledTransactionMessageDecoder)().decode(transaction.messageBytes);
        if (dMessage.version === 'legacy') {
            return [];
        }
        return dMessage.addressTableLookups?.map((lut) => lut.lookupTableAddress) ?? [];
    }
    catch (error) {
        console.warn('Failed to extract LUT addresses from transaction:', error);
        return [];
    }
}
//# sourceMappingURL=compat.js.map