"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.base64ToTransaction = base64ToTransaction;
exports.extractInstructionsAndLookupTablesFromTxn = extractInstructionsAndLookupTablesFromTxn;
// Function to convert base64 to VersionedTransaction
const index_1 = require("./index");
const kit_1 = require("@solana/kit");
function base64ToTransaction(base64Txn) {
    // Decode base64 into a Uint8Array
    const txnBuffer = Buffer.from(base64Txn, 'base64');
    // Deserialize the transaction
    return (0, kit_1.getTransactionDecoder)().decode(txnBuffer);
}
async function extractInstructionsAndLookupTablesFromTxn(connection, txn) {
    const dMessage = (0, kit_1.getCompiledTransactionMessageDecoder)().decode(txn.messageBytes);
    let lutAddresses = [];
    if (dMessage.version !== 'legacy') {
        lutAddresses = dMessage.addressTableLookups?.map((lut) => lut.lookupTableAddress) ?? [];
    }
    let decompiled;
    let lutAccounts = [];
    if (lutAddresses.length) {
        lutAccounts = await (0, index_1.getAllLookupTables)(connection, lutAddresses);
        const addresses = (0, index_1.transformFromLookupTableAccounts)(lutAccounts);
        decompiled = (0, kit_1.decompileTransactionMessage)(dMessage, { addressesByLookupTableAddress: addresses });
    }
    else {
        decompiled = (0, kit_1.decompileTransactionMessage)(dMessage);
    }
    const instructions = decompiled.instructions.map((ix) => ({ ...ix }));
    return { instructions, lookupTables: lutAccounts };
}
//# sourceMappingURL=decodeTransaction.js.map