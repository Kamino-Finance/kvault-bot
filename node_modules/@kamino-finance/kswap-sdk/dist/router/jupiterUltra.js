"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.JupiterUltraRouter = void 0;
exports.executeJupiterZTransaction = executeJupiterZTransaction;
exports.fetchJupiterPrice = fetchJupiterPrice;
const swap_api_utils_1 = require("../swap_api_utils");
const axios_1 = __importDefault(require("axios"));
const bn_js_1 = __importDefault(require("bn.js"));
const consts_1 = require("../consts");
const utils_1 = require("../utils");
const decodeTransaction_1 = require("../utils/decodeTransaction");
class JupiterUltraRouter {
    connection;
    baseUrl = consts_1.JUP_ULTRA_BASE_URL;
    routerType;
    logger;
    constructor(connection, logger) {
        this.connection = connection;
        this.routerType = 'jupiterU';
        this.logger = logger;
    }
    async quote(params) {
        try {
            if (params.swapType === 'exactOut') {
                return undefined;
            }
            const timeBeforeGetQuoteData = Date.now();
            const swapQuote = await this.getSwapQuote({
                inputMint: params.tokenIn.toString(),
                outputMint: params.tokenOut.toString(),
                amount: (0, swap_api_utils_1.safeBNToNumber)(params.amount),
            });
            const timeAfterGetQuoteData = Date.now();
            if (swapQuote.inAmount !== params.amount.toString()) {
                return undefined;
            }
            return [this.buildRouteOutput(swapQuote, params.swapType, timeAfterGetQuoteData - timeBeforeGetQuoteData, 0)];
        }
        catch (error) {
            this.logger.error(`JupUltra: Failed to get quote`, error);
            return undefined;
        }
    }
    async route(params, ctx) {
        try {
            if (params.swapType === 'exactOut') {
                return undefined;
            }
            if (params.destinationTokenAccount) {
                return undefined;
            }
            if (params.wrapAndUnwrapSol === false || params.includeSetupIxs === false) {
                return undefined;
            }
            const timeBeforeGetQuote = Date.now();
            const swapQuoteResponse = await this.getSwapQuote({
                inputMint: params.tokenIn.toString(),
                outputMint: params.tokenOut.toString(),
                amount: (0, swap_api_utils_1.safeBNToNumber)(params.amount),
                taker: params.executor.toString(),
            });
            const timeAfterGetQuote = Date.now();
            if (swapQuoteResponse.inAmount !== params.amount.toString()) {
                return undefined;
            }
            if (!swapQuoteResponse.transaction) {
                this.logger.error(`JupUltra: Swap transaction is null`);
                return undefined;
            }
            const priceImpactBps = Number(swapQuoteResponse.priceImpactPct) * 100;
            const transaction = (0, decodeTransaction_1.base64ToTransaction)(swapQuoteResponse.transaction);
            let instructionsWithoutCu = undefined;
            let lookupTablesAccounts = undefined;
            // In case of aggregator swap, we need to keep the transaction and execute it
            const { instructions, lookupTables } = await (0, decodeTransaction_1.extractInstructionsAndLookupTablesFromTxn)(this.connection, transaction);
            instructionsWithoutCu = (0, utils_1.removeComputeBudgetProgram)(instructions);
            let foundValidSwap = false;
            for (const ix of instructionsWithoutCu) {
                if (ix.programAddress === consts_1.ROUTER_PROGRAM_MAP['jupiterU'] ||
                    ix.programAddress === consts_1.ROUTER_PROGRAM_MAP['jupiterZ']) {
                    foundValidSwap = true;
                    break;
                }
            }
            if (!foundValidSwap) {
                return undefined;
            }
            lookupTablesAccounts = lookupTables;
            const timeAfterSwapIxs = Date.now();
            return [
                {
                    ...this.buildRouteOutput(swapQuoteResponse, params.swapType, timeAfterGetQuote - timeBeforeGetQuote, timeAfterSwapIxs - timeAfterGetQuote),
                    transaction: transaction,
                    ixsRouter: instructionsWithoutCu,
                    lookupTableAccounts: lookupTablesAccounts,
                    priceImpactBps,
                },
            ];
        }
        catch (error) {
            this.logger.error(`${this.routerType}: Failed to route swap`, error);
            return undefined;
        }
    }
    async getSwapQuote(params) {
        try {
            const response = await axios_1.default.get(`${this.baseUrl}/order`, {
                params,
                headers: {},
            });
            return response.data;
        }
        catch (error) {
            this.logger.error(`${this.routerType}: Failed to fetch swap quote`, error);
            throw new Error(`${this.routerType}: Failed to fetch swap quote`, error);
        }
    }
    buildRouteOutput(swapQuote, swapType, responseTimeGetQuoteMs, responseTimeSwapIxsMs) {
        return {
            amountsExactIn: {
                amountIn: new bn_js_1.default(swapQuote.inAmount),
                amountOutGuaranteed: new bn_js_1.default(swapQuote.otherAmountThreshold),
                amountOut: new bn_js_1.default(swapQuote.outAmount),
            },
            amountsExactOut: {
                amountOut: new bn_js_1.default(0),
                amountInGuaranteed: new bn_js_1.default(0),
                amountIn: new bn_js_1.default(0),
            },
            swapType: swapType,
            responseTimeGetQuoteMs,
            responseTimeSwapIxsMs,
            routerType: swapQuote.swapType === 'aggregator' ? 'jupiterU' : 'jupiterZ',
            expiryTime: 0,
            jupRequestId: swapQuote.requestId,
        };
    }
}
exports.JupiterUltraRouter = JupiterUltraRouter;
async function executeJupiterZTransaction(params) {
    try {
        const response = await axios_1.default.post(`${consts_1.JUP_ULTRA_BASE_URL}/execute`, {
            signedTransaction: params.signedTransaction,
            requestId: params.requestId,
        }, {
            headers: {
                'Content-Type': 'application/json',
                Accept: 'application/json',
            },
        });
        return response.data;
    }
    catch (error) {
        params.logger.error('Execute transaction error:', error);
        throw new Error(`Failed to execute transaction`, error);
    }
}
async function fetchJupiterPrice(query) {
    const response = await axios_1.default.get(consts_1.JUPITER_PRICE_API, {
        params: query,
        headers: { 'Content-Type': 'application/json' },
    });
    return response.data;
}
//# sourceMappingURL=jupiterUltra.js.map