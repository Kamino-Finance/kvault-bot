import { QuoteResponse } from '@jup-ag/api';
import BN from 'bn.js';
import { JupiterBase } from './jupiterInterfaceBase';
import { QuoteParams, RouteOutput, RouteParams, RouterContext } from '../swap_api_utils';
import { RouterType } from '../consts';
import { Logger } from '../utils/Logger';
import { Address, Rpc, SolanaRpcApi } from '@solana/kit';
export declare abstract class JupiterRouterBase extends JupiterBase {
    private maxAccounts;
    private jupiterClient;
    private logger;
    constructor(connection: Rpc<SolanaRpcApi>, routerType: RouterType, jupiterUrl: string, maxAccounts: number, logger?: Logger);
    route(params: RouteParams, ctx: RouterContext): Promise<RouteOutput[] | undefined>;
    quote(params: QuoteParams): Promise<RouteOutput[] | undefined>;
    getQuoteResponse(tokenIn: Address, tokenOut: Address, amountIn: BN, maxSlippageBps: number): Promise<QuoteResponse>;
}
export declare function getJupiterPrice(inputMint: Address, outputMint: Address): Promise<number>;
//# sourceMappingURL=jupiterRouterBase.d.ts.map