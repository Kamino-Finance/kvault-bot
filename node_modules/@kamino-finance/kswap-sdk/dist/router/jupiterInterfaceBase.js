"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.JupiterBase = void 0;
const bn_js_1 = __importDefault(require("bn.js"));
const swap_api_utils_1 = require("../swap_api_utils");
const utils_1 = require("../utils");
const limo_sdk_1 = require("@kamino-finance/limo-sdk");
const kit_1 = require("@solana/kit");
class JupiterBase {
    routerType;
    connection;
    constructor(routerType, connection) {
        this.routerType = routerType;
        this.connection = connection;
    }
    async getRouteOutput(instructions, quoteResponse, params, timeAfterGetQuote, timeBeforeGetQuote) {
        const { setupInstructions, swapInstruction, addressLookupTableAddresses } = instructions;
        const lookupTablePubkeys = addressLookupTableAddresses.map((addr) => (0, kit_1.address)(addr));
        const lookupTableAccounts = await (0, utils_1.getAllLookupTables)(this.connection, lookupTablePubkeys);
        const timeAfterSwapIxsRequest = Date.now();
        const ixsSetupJupiter = setupInstructions.map((ix) => this.convertJupiterToTransactionInstruction(ix));
        const ixsJupiter = [
            ...(params.includeSetupIxs ? ixsSetupJupiter : []),
            this.convertJupiterToTransactionInstruction(swapInstruction),
        ];
        const filteredIxsJupiter = (0, utils_1.removeComputeBudgetIxns)(ixsJupiter);
        const priceImpactBps = Number(quoteResponse.priceImpactPct) * 100;
        if (params.wrapAndUnwrapSol) {
            if (params.tokenIn === limo_sdk_1.WRAPPED_SOL_MINT) {
                filteredIxsJupiter.push(await (0, swap_api_utils_1.unwrapSolIx)(params, undefined));
            }
            else if (params.tokenOut === limo_sdk_1.WRAPPED_SOL_MINT) {
                filteredIxsJupiter.push(await (0, swap_api_utils_1.unwrapSolIx)(params, params.destinationTokenAccount));
            }
        }
        return {
            ixsRouter: filteredIxsJupiter,
            amountsExactIn: {
                amountIn: params.swapType === 'exactIn' ? params.amount : new bn_js_1.default(0),
                amountOutGuaranteed: params.swapType === 'exactIn' ? new bn_js_1.default(quoteResponse.otherAmountThreshold) : new bn_js_1.default(0),
                amountOut: params.swapType === 'exactIn' ? new bn_js_1.default(quoteResponse.outAmount) : new bn_js_1.default(0),
            },
            amountsExactOut: {
                amountOut: params.swapType === 'exactOut' ? params.amount : new bn_js_1.default(0),
                amountInGuaranteed: params.swapType === 'exactOut' ? new bn_js_1.default(quoteResponse.otherAmountThreshold.toString()) : new bn_js_1.default(0),
                amountIn: params.swapType === 'exactOut' ? new bn_js_1.default(quoteResponse.inAmount.toString()) : new bn_js_1.default(0),
            },
            swapType: params.swapType,
            responseTimeGetQuoteMs: timeAfterGetQuote - timeBeforeGetQuote,
            responseTimeSwapIxsMs: timeAfterSwapIxsRequest - timeAfterGetQuote,
            lookupTableAccounts: lookupTableAccounts,
            routerType: this.routerType,
            expiryTime: 0,
            priceImpactBps,
        };
    }
    async getQuoteOutput(quoteResponse, params, timeAfterGetQuoteData, timeBeforeGetQuoteData) {
        const priceImpactBps = Number(quoteResponse.priceImpactPct) * 100;
        return {
            amountsExactIn: {
                amountIn: params.swapType === 'exactIn' ? params.amount : new bn_js_1.default(0),
                amountOutGuaranteed: params.swapType === 'exactIn' ? new bn_js_1.default(quoteResponse.otherAmountThreshold) : new bn_js_1.default(0),
                amountOut: params.swapType === 'exactIn' ? new bn_js_1.default(quoteResponse.outAmount) : new bn_js_1.default(0),
            },
            amountsExactOut: {
                amountOut: params.swapType === 'exactOut' ? params.amount : new bn_js_1.default(0),
                amountInGuaranteed: params.swapType === 'exactOut' ? new bn_js_1.default(quoteResponse.otherAmountThreshold.toString()) : new bn_js_1.default(0),
                amountIn: params.swapType === 'exactOut' ? new bn_js_1.default(quoteResponse.outAmount.toString()) : new bn_js_1.default(0),
            },
            swapType: params.swapType,
            responseTimeGetQuoteMs: timeAfterGetQuoteData - timeBeforeGetQuoteData,
            responseTimeSwapIxsMs: 0,
            routerType: this.routerType,
            expiryTime: 0,
            priceImpactBps,
        };
    }
    convertJupiterToTransactionInstruction(instruction) {
        return {
            programAddress: (0, kit_1.address)(instruction.programId),
            accounts: instruction.accounts.map((key) => ({
                address: (0, kit_1.address)(key.pubkey),
                role: (0, utils_1.determineRole)(key.isSigner, key.isWritable),
            })),
            data: Buffer.from(instruction.data, 'base64'),
        };
    }
}
exports.JupiterBase = JupiterBase;
//# sourceMappingURL=jupiterInterfaceBase.js.map