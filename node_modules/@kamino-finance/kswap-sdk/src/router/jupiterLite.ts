import { Router } from '../swap_api_utils';
import { JUP_V6_BASE_URL } from '../consts';
import { JupiterRouterBase } from './jupiterRouterBase';
import { Logger } from '../utils/Logger';
import { Rpc, SolanaRpcApi } from '@solana/kit';

export class JupiterRouterLite extends JupiterRouterBase implements Router {
  constructor(connection: Rpc<SolanaRpcApi>, maxAccounts: number, logger: Logger) {
    super(connection, 'jupiterLite', JUP_V6_BASE_URL, maxAccounts, logger);
  }
}
