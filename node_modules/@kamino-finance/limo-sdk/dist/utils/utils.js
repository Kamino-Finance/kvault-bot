"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.TokenInfo = exports.DEFAULT_TXN_FEE_LAMPORTS = exports.DEFAULT_ADDRESS = void 0;
exports.getLimoProgramId = getLimoProgramId;
exports.parseKeypairFile = parseKeypairFile;
exports.divCeil = divCeil;
exports.amountToLamportsBN = amountToLamportsBN;
exports.amountToLamportsDecimal = amountToLamportsDecimal;
exports.lamportsToAmountBN = lamportsToAmountBN;
exports.lamportsToAmountDecimal = lamportsToAmountDecimal;
exports.createMint = createMint;
exports.createMintFromKeypair = createMintFromKeypair;
exports.getMintDecimals = getMintDecimals;
exports.printMultisigTx = printMultisigTx;
exports.printSimulateTx = printSimulateTx;
exports.solAirdrop = solAirdrop;
exports.checkIfAccountExists = checkIfAccountExists;
exports.getAssociatedTokenAddress = getAssociatedTokenAddress;
exports.createAssociatedTokenAccountIdempotentInstruction = createAssociatedTokenAccountIdempotentInstruction;
exports.createAtaIdempotent = createAtaIdempotent;
exports.setupAta = setupAta;
exports.mintTo = mintTo;
exports.getMockSwapInstructions = getMockSwapInstructions;
exports.getMockSwapSingleInstructionSplOnly = getMockSwapSingleInstructionSplOnly;
exports.getTokenAccountBalance = getTokenAccountBalance;
exports.createAddExtraComputeUnitFeeTransaction = createAddExtraComputeUnitFeeTransaction;
exports.getSolBalanceInLamports = getSolBalanceInLamports;
exports.getSolBalance = getSolBalance;
exports.endpointFromCluster = endpointFromCluster;
exports.subscriptionEndpointFromCluster = subscriptionEndpointFromCluster;
exports.fetchGlobalConfigWithRetry = fetchGlobalConfigWithRetry;
exports.getPdaAuthority = getPdaAuthority;
exports.getTokenVaultPDA = getTokenVaultPDA;
exports.getEventAuthorityPDA = getEventAuthorityPDA;
exports.getUserSwapBalanceStatePDA = getUserSwapBalanceStatePDA;
exports.getUserSwapBalanceAssertStatePDA = getUserSwapBalanceAssertStatePDA;
exports.getIntermediaryTokenAccountPDA = getIntermediaryTokenAccountPDA;
exports.getExpressRelayMetadataPDA = getExpressRelayMetadataPDA;
exports.getExpressRelayConfigRouterPDA = getExpressRelayConfigRouterPDA;
exports.getOrderRentExemptLamports = getOrderRentExemptLamports;
exports.createKeypairRentExemptIxSync = createKeypairRentExemptIxSync;
exports.executeTransaction = executeTransaction;
exports.sleep = sleep;
exports.asOption = asOption;
const compute_budget_1 = require("@solana-program/compute-budget");
const decimal_js_1 = require("decimal.js");
const Limo_1 = require("../Limo");
const accounts_1 = require("../rpc_client/accounts");
const programId_1 = require("../rpc_client/programId");
// @ts-ignore
const accounts_2 = require("../rpc_client/accounts");
const bn_js_1 = __importDefault(require("bn.js"));
const kit_1 = require("@solana/kit");
const system_1 = require("@solana-program/system");
const token_1 = require("@solana-program/token");
const signers_1 = require("@solana/signers");
const token_2022_1 = require("@solana-program/token-2022");
exports.DEFAULT_ADDRESS = (0, kit_1.address)("11111111111111111111111111111111");
exports.DEFAULT_TXN_FEE_LAMPORTS = 5000;
const ESCROW_VAULT_SEED = "escrow_vault";
const GLOBAL_AUTH_SEED = "authority";
const EXPRESS_RELAY_MEATADATA_SEED = "metadata";
const EXPRESS_RELAY_CONFIG_ROUTER_SEED = "config_router";
const INTERMEDIARY_OUTPUT_TOKEN_ACCOUNT_SEED = "intermediary";
const EVENT_AUTHORITY_SEED = "__event_authority";
class TokenInfo {
    symbol;
    mint;
    mintDecimals;
    constructor(symbol, mint, mintDecimal) {
        this.symbol = symbol;
        this.mint = mint;
        this.mintDecimals = mintDecimal;
    }
}
exports.TokenInfo = TokenInfo;
function getLimoProgramId() {
    return programId_1.PROGRAM_ID;
}
async function parseKeypairFile(file) {
    const keypairFile = require("fs").readFileSync(file);
    const keypairBytes = new Uint8Array(JSON.parse(keypairFile.toString()));
    const keypair = await (0, kit_1.createKeyPairFromBytes)(keypairBytes);
    return (0, kit_1.createSignerFromKeyPair)(keypair);
}
function divCeil(a, b) {
    return a.add(b).sub(new bn_js_1.default(1)).div(b);
}
function amountToLamportsBN(amount, decimals) {
    let factor = Math.pow(10, decimals);
    return new bn_js_1.default(amount.mul(factor).floor().toString());
}
function amountToLamportsDecimal(amount, decimals) {
    let factor = Math.pow(10, decimals);
    return amount.mul(factor);
}
function lamportsToAmountBN(amount, decimals) {
    let factor = new bn_js_1.default(Math.pow(10, decimals));
    return new decimal_js_1.Decimal(amount.div(factor).toString());
}
function lamportsToAmountDecimal(amount, decimals) {
    let factor = Math.pow(10, decimals);
    return amount.div(factor);
}
async function createMint(rpc, rpcWs, authority, decimals = 6) {
    const mint = await (0, signers_1.generateKeyPairSigner)();
    return await createMintFromKeypair(rpc, rpcWs, authority, mint, decimals);
}
async function createMintFromKeypair(rpc, rpcWs, authority, mint, decimals = 6) {
    const instructions = await createMintInstructions(rpc, authority, mint, decimals);
    console.log("Mint auth", authority.address);
    const { value: latestBlockhash } = await rpc.getLatestBlockhash().send();
    const transactionMessage = (0, kit_1.pipe)((0, kit_1.createTransactionMessage)({ version: 0 }), // Create transaction message
    (tx) => (0, kit_1.setTransactionMessageFeePayerSigner)(authority, tx), (tx) => (0, kit_1.setTransactionMessageLifetimeUsingBlockhash)(latestBlockhash, tx), (tx) => (0, kit_1.appendTransactionMessageInstructions)(instructions, tx));
    const signedTransaction = await (0, kit_1.signTransactionMessageWithSigners)(transactionMessage);
    await (0, kit_1.sendAndConfirmTransactionFactory)({ rpc, rpcSubscriptions: rpcWs })(signedTransaction, { commitment: "confirmed" });
    return mint.address;
}
async function getMintDecimals(rpc, mint) {
    let info = await (0, token_2022_1.fetchMint)(rpc, mint);
    return info.data.decimals;
}
async function createMintInstructions(rpc, authority, mint, decimals) {
    return [
        (0, system_1.getCreateAccountInstruction)({
            payer: authority,
            newAccount: mint,
            lamports: await rpc.getMinimumBalanceForRentExemption(BigInt(82)).send(),
            space: 82,
            programAddress: token_1.TOKEN_PROGRAM_ADDRESS,
        }),
        (0, token_1.getInitializeMintInstruction)({
            mint: mint.address,
            decimals: decimals,
            mintAuthority: authority.address,
        }),
    ];
}
async function printMultisigTx(tx) {
    console.log((0, kit_1.getBase58Decoder)().decode(tx.messageBytes));
}
async function printSimulateTx(rpc, tx) {
    console.log("Tx in B64", `https://explorer.solana.com/tx/inspector?message=${encodeURIComponent(Buffer.from(tx.messageBytes).toString("base64"))}`);
    let res = await rpc
        .simulateTransaction((0, kit_1.getBase64EncodedWireTransaction)(tx), {
        encoding: "base64",
    })
        .send();
    console.log("Simulate Response", res.value);
    console.log("");
}
async function solAirdrop(rpc, rpcWs, account, solAirdrop) {
    await (0, kit_1.airdropFactory)({ rpc, rpcSubscriptions: rpcWs })({
        recipientAddress: account,
        lamports: (0, kit_1.lamports)(BigInt(amountToLamportsDecimal(solAirdrop, 9).toString())),
        commitment: "confirmed",
    });
    return await getSolBalance(rpc, account);
}
async function checkIfAccountExists(rpc, account) {
    const acc = await (0, kit_1.fetchEncodedAccount)(rpc, account);
    return acc.exists;
}
async function getAssociatedTokenAddress(ownerAddress, tokenMintAddress, tokenProgramAddress = token_1.TOKEN_PROGRAM_ADDRESS) {
    const [associatedTokenAddress] = await (0, token_1.findAssociatedTokenPda)({
        mint: tokenMintAddress,
        owner: ownerAddress,
        tokenProgram: tokenProgramAddress,
    });
    return associatedTokenAddress;
}
async function createAssociatedTokenAccountIdempotentInstruction(owner, mint, payer, tokenProgramAddress = token_1.TOKEN_PROGRAM_ADDRESS, ata) {
    const actualPayer = payer || owner;
    const ownerAddress = typeof owner === "string" ? owner : owner.address;
    let ataAddress = ata;
    if (!ataAddress) {
        ataAddress = await getAssociatedTokenAddress(ownerAddress, mint, tokenProgramAddress);
    }
    const createUserTokenAccountIx = await (0, token_1.getCreateAssociatedTokenInstructionAsync)({
        payer: actualPayer,
        mint,
        owner: ownerAddress,
    });
    // - discriminator 0: regular create (fails if account exists)
    // - discriminator 1: idempotent create (succeeds if account exists)
    const IDEMPOTENT_CREATE_DISCRIMINATOR = 1;
    const idempotentCreateUserTokenAccountIx = {
        ...createUserTokenAccountIx,
        data: new Uint8Array([
            IDEMPOTENT_CREATE_DISCRIMINATOR,
            ...createUserTokenAccountIx.data.slice(1),
        ]),
    };
    return [ataAddress, idempotentCreateUserTokenAccountIx];
}
async function createAtaIdempotent(user, payer, mint, mintTokenProgramAddress = token_1.TOKEN_PROGRAM_ADDRESS) {
    const [ata, createAtaIx] = await createAssociatedTokenAccountIdempotentInstruction(user, mint, payer, mintTokenProgramAddress);
    return { ata, createAtaIx };
}
async function setupAta(rpc, rpcWs, tokenMintAddress, payer, owner = payer) {
    const ata = await getAssociatedTokenAddress(owner.address, tokenMintAddress);
    const accountExists = await checkIfAccountExists(rpc, ata);
    if (!accountExists) {
        const [, ix] = await createAssociatedTokenAccountIdempotentInstruction(owner.address, tokenMintAddress, payer, token_1.TOKEN_PROGRAM_ADDRESS, ata);
        const { value: latestBlockhash } = await rpc.getLatestBlockhash().send();
        const transactionMessage = (0, kit_1.pipe)((0, kit_1.createTransactionMessage)({ version: 0 }), (tx) => (0, kit_1.setTransactionMessageFeePayerSigner)(payer, tx), (tx) => (0, kit_1.setTransactionMessageLifetimeUsingBlockhash)(latestBlockhash, tx), (tx) => (0, kit_1.appendTransactionMessageInstructions)([ix], tx));
        const signedTransaction = await (0, kit_1.signTransactionMessageWithSigners)(transactionMessage);
        await (0, kit_1.sendAndConfirmTransactionFactory)({ rpc, rpcSubscriptions: rpcWs })(signedTransaction, { commitment: "confirmed" });
    }
    return ata;
}
async function mintTo(rpc, rpcWs, payer, mint, tokenAccount, amount, decimals) {
    // new implementation
    const { value: latestBlockhash } = await rpc.getLatestBlockhash().send();
    const ix = (0, token_1.getMintToCheckedInstruction)({
        mint, // mint
        token: tokenAccount, // receiver (sholud be a token account)
        mintAuthority: payer, // mint authority
        amount, // amount. if your decimals is 8, you mint 10^8 for 1 token.
        decimals,
    });
    const transactionMessage = (0, kit_1.pipe)((0, kit_1.createTransactionMessage)({ version: 0 }), (tx) => (0, kit_1.setTransactionMessageFeePayerSigner)(payer, tx), (tx) => (0, kit_1.setTransactionMessageLifetimeUsingBlockhash)(latestBlockhash, tx), (tx) => (0, kit_1.appendTransactionMessageInstructions)([ix], tx));
    const signedTransaction = await (0, kit_1.signTransactionMessageWithSigners)(transactionMessage);
    await (0, kit_1.sendAndConfirmTransactionFactory)({
        rpc,
        rpcSubscriptions: rpcWs,
    })(signedTransaction, { commitment: "confirmed", skipPreflight: true });
}
async function getMockSwapInstructions(authority, user, swapInputMint, swapOutputMint, swapInAmountDecimal, swapOutAmountDecimal, inTokenDecimals, outTokenDecimals) {
    let mintToIx = [];
    if (swapOutputMint === Limo_1.WRAPPED_SOL_MINT) {
        // transfer WSOL to user
        mintToIx.push((0, system_1.getTransferSolInstruction)({
            source: authority,
            destination: await getAssociatedTokenAddress(user.address, swapOutputMint),
            amount: amountToLamportsDecimal(swapOutAmountDecimal, outTokenDecimals).toNumber(),
        }));
        mintToIx.push({
            programAddress: token_1.TOKEN_PROGRAM_ADDRESS,
            accounts: [
                {
                    address: await getAssociatedTokenAddress(user.address, swapOutputMint),
                    role: kit_1.AccountRole.WRITABLE,
                },
            ],
            data: new Uint8Array([17]),
        });
    }
    else {
        mintToIx.push((0, token_1.getMintToCheckedInstruction)({
            mint: swapOutputMint,
            token: await getAssociatedTokenAddress(user.address, swapOutputMint),
            mintAuthority: authority,
            amount: amountToLamportsDecimal(swapOutAmountDecimal, outTokenDecimals).toNumber(),
            decimals: outTokenDecimals,
        }));
    }
    let burnIx = [];
    if (swapInputMint === Limo_1.WRAPPED_SOL_MINT) {
        burnIx.push((0, token_1.getCloseAccountInstruction)({
            account: await getAssociatedTokenAddress(user.address, swapInputMint),
            destination: user.address,
            owner: user.address,
        })); // close WSOL ATA so sol is back to native
        burnIx.push((0, system_1.getTransferSolInstruction)({
            source: user,
            destination: authority.address,
            amount: amountToLamportsDecimal(swapInAmountDecimal, inTokenDecimals).toNumber(),
        }));
        burnIx.push((await createAtaIdempotent(user.address, authority, Limo_1.WRAPPED_SOL_MINT))
            .createAtaIx); //reinitialise WSOL ATA as this is supposed to exist
    }
    else {
        burnIx.push((0, token_1.getBurnInstruction)({
            account: await getAssociatedTokenAddress(user.address, swapInputMint),
            mint: swapInputMint,
            authority: user,
            amount: amountToLamportsDecimal(swapInAmountDecimal, inTokenDecimals).toNumber(),
        }));
    }
    return [...burnIx, ...mintToIx];
}
async function getMockSwapSingleInstructionSplOnly(authority, user, swapOutputMint, swapOutAmountDecimal, outTokenDecimals) {
    return (0, token_1.getMintToCheckedInstruction)({
        mint: swapOutputMint,
        token: await getAssociatedTokenAddress(user, swapOutputMint),
        mintAuthority: authority,
        amount: amountToLamportsDecimal(swapOutAmountDecimal, outTokenDecimals).toNumber(),
        decimals: outTokenDecimals,
    });
}
async function getTokenAccountBalance(rpc, tokenAccount) {
    const tokenAccountBalance = await rpc
        .getTokenAccountBalance(tokenAccount)
        .send();
    return new decimal_js_1.Decimal(tokenAccountBalance.value.amount).div(decimal_js_1.Decimal.pow(10, tokenAccountBalance.value.decimals));
}
function createAddExtraComputeUnitFeeTransaction(units, microLamports) {
    const ixs = [];
    ixs.push((0, compute_budget_1.getSetComputeUnitLimitInstruction)({ units }));
    ixs.push((0, compute_budget_1.getSetComputeUnitPriceInstruction)({
        microLamports: Number(microLamports.toFixed(0)),
    }));
    return ixs;
}
async function getSolBalanceInLamports(rpc, account) {
    let balance = undefined;
    while (balance === undefined) {
        balance = (await rpc.getAccountInfo(account).send())?.value?.lamports;
    }
    return balance;
}
async function getSolBalance(rpc, account) {
    const balance = new decimal_js_1.Decimal((await getSolBalanceInLamports(rpc, account)).toString());
    return lamportsToAmountDecimal(balance, 9);
}
function endpointFromCluster(cluster) {
    switch (cluster) {
        case "mainnet":
            return "FIXTHIS";
        case "devnet":
            return "FIXTHIS";
        case "localnet":
            return "http://127.0.0.1:8899";
    }
    if (cluster) {
        return cluster;
    }
    return "err";
}
function subscriptionEndpointFromCluster(cluster) {
    switch (cluster) {
        case "mainnet":
            return "FIXTHIS";
        case "devnet":
            return "FIXTHIS";
        case "localnet":
            return "ws://127.0.0.1:8900";
    }
    if (cluster) {
        return cluster;
    }
    return "err";
}
async function fetchGlobalConfigWithRetry(rpc, address) {
    return fetchWithRetry(async () => await accounts_1.GlobalConfig.fetch(rpc, address), address);
}
async function getPdaAuthority(programAddress, globalConfig) {
    const addressEncoder = (0, kit_1.getAddressEncoder)();
    const [pdaAuthority, _pdaAuthorityBump] = await (0, kit_1.getProgramDerivedAddress)({
        programAddress,
        seeds: [GLOBAL_AUTH_SEED, addressEncoder.encode(globalConfig)],
    });
    return pdaAuthority;
}
async function getTokenVaultPDA(programAddress, globalConfig, mint) {
    const addressEncoder = (0, kit_1.getAddressEncoder)();
    const [vault, _vaultBump] = await (0, kit_1.getProgramDerivedAddress)({
        programAddress,
        seeds: [
            ESCROW_VAULT_SEED,
            addressEncoder.encode(globalConfig),
            addressEncoder.encode(mint),
        ],
    });
    return vault;
}
async function getEventAuthorityPDA(programAddress) {
    const [eventAuthority, _eventAuthorityBump] = await (0, kit_1.getProgramDerivedAddress)({
        programAddress,
        seeds: [EVENT_AUTHORITY_SEED],
    });
    return eventAuthority;
}
async function getUserSwapBalanceStatePDA(maker, programAddress) {
    const addressEncoder = (0, kit_1.getAddressEncoder)();
    const [swapBalanceState, _swapBalanceStateBump] = await (0, kit_1.getProgramDerivedAddress)({
        programAddress,
        seeds: ["balances", addressEncoder.encode(maker)],
    });
    return swapBalanceState;
}
async function getUserSwapBalanceAssertStatePDA(maker, programAddress) {
    const addressEncoder = (0, kit_1.getAddressEncoder)();
    const [swapBalanceState, _swapBalanceStateBump] = await (0, kit_1.getProgramDerivedAddress)({
        programAddress,
        seeds: ["assert_swap", addressEncoder.encode(maker)],
    });
    return swapBalanceState;
}
async function getIntermediaryTokenAccountPDA(programAddress, order) {
    const addressEncoder = (0, kit_1.getAddressEncoder)();
    const [intermediaryTokenAccount, _intermediaryTokenAccount] = await (0, kit_1.getProgramDerivedAddress)({
        programAddress,
        seeds: [
            INTERMEDIARY_OUTPUT_TOKEN_ACCOUNT_SEED,
            addressEncoder.encode(order),
        ],
    });
    return intermediaryTokenAccount;
}
async function getExpressRelayMetadataPDA(programAddress) {
    const [metadata, _metadataBump] = await (0, kit_1.getProgramDerivedAddress)({
        programAddress,
        seeds: [EXPRESS_RELAY_MEATADATA_SEED],
    });
    return metadata;
}
async function getExpressRelayConfigRouterPDA(programAddress, router) {
    const addressEncoder = (0, kit_1.getAddressEncoder)();
    const [configRouter, _configRouterBump] = await (0, kit_1.getProgramDerivedAddress)({
        programAddress,
        seeds: [EXPRESS_RELAY_CONFIG_ROUTER_SEED, addressEncoder.encode(router)],
    });
    return configRouter;
}
async function fetchWithRetry(fetch, address, retries = 3) {
    for (let i = 0; i < retries; i++) {
        let resp = await fetch();
        if (resp !== null) {
            return resp;
        }
        console.log(`[${i + 1}/${retries}] Fetched account ${address} is null. Refetching...`);
    }
    return null;
}
async function getOrderRentExemptLamports(rpc) {
    return await rpc
        .getMinimumBalanceForRentExemption(BigInt(accounts_2.Order.layout.span + 8))
        .send();
}
function createKeypairRentExemptIxSync(payer, account, size, lamports, programAddress = Limo_1.limoId) {
    return (0, system_1.getCreateAccountInstruction)({
        payer: payer,
        newAccount: account,
        lamports: lamports,
        space: size,
        programAddress: programAddress,
    });
}
async function executeTransaction(rpc, rpcWs, ixns, signer, extraSigners = []) {
    const { value: latestBlockhash } = await rpc.getLatestBlockhash().send();
    const transactionMessage = (0, kit_1.pipe)((0, kit_1.createTransactionMessage)({ version: 0 }), (tx) => (0, kit_1.setTransactionMessageFeePayerSigner)(signer, tx), (tx) => (0, kit_1.setTransactionMessageLifetimeUsingBlockhash)(latestBlockhash, tx), (tx) => (0, kit_1.appendTransactionMessageInstructions)(ixns, tx));
    const transactionsMessageWithSigners = extraSigners.length
        ? (0, signers_1.addSignersToTransactionMessage)(extraSigners, transactionMessage)
        : transactionMessage;
    const signedTransaction = await (0, kit_1.signTransactionMessageWithSigners)(transactionsMessageWithSigners);
    await (0, kit_1.sendAndConfirmTransactionFactory)({ rpc, rpcSubscriptions: rpcWs })(signedTransaction, { commitment: "confirmed" });
    return (0, kit_1.getSignatureFromTransaction)(signedTransaction);
}
function sleep(ms) {
    return new Promise((resolve) => setTimeout(resolve, ms));
}
function asOption(address) {
    return address
        ? {
            value: address,
            __option: "Some",
        }
        : { __option: "None" };
}
//# sourceMappingURL=utils.js.map