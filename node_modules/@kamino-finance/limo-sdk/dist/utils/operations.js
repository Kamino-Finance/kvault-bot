"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.initializeGlobalConfig = initializeGlobalConfig;
exports.initializeVault = initializeVault;
exports.createOrder = createOrder;
exports.updateOrder = updateOrder;
exports.takeOrder = takeOrder;
exports.flashTakeOrder = flashTakeOrder;
exports.closeOrderAndClaimTip = closeOrderAndClaimTip;
exports.withdrawHostTipIx = withdrawHostTipIx;
exports.updateGlobalConfigIx = updateGlobalConfigIx;
exports.updateGlobalConfigAdminIx = updateGlobalConfigAdminIx;
const Instructions = __importStar(require("../rpc_client/instructions"));
const utils_1 = require("./utils");
const index_1 = require("../rpc_client/types/index");
const kit_1 = require("@solana/kit");
const system_1 = require("@solana-program/system");
const sysvars_1 = require("@solana/sysvars");
const bn_js_1 = __importDefault(require("bn.js"));
function initializeGlobalConfig(adminAuthority, globalConfig, pdaAuthority, programId) {
    let accounts = {
        adminAuthority,
        pdaAuthority,
        globalConfig,
    };
    let ix = Instructions.initializeGlobalConfig(accounts, [], programId);
    return ix;
}
async function initializeVault(owner, globalConfig, mint, programAddress, mintProgramId) {
    let vault = await (0, utils_1.getTokenVaultPDA)(programAddress, globalConfig, mint);
    let pdaAuthority = await (0, utils_1.getPdaAuthority)(programAddress, globalConfig);
    let accounts = {
        payer: owner,
        globalConfig,
        pdaAuthority,
        mint,
        vault,
        tokenProgram: mintProgramId,
        systemProgram: system_1.SYSTEM_PROGRAM_ADDRESS,
    };
    let ix = Instructions.initializeVault(accounts, [], programAddress);
    return ix;
}
async function createOrder(maker, globalConfig, order, orderParams, programAddress, baseTokenMintProgramId, quoteTokenMintProgramId) {
    let quoteTokenVault = await (0, utils_1.getTokenVaultPDA)(programAddress, globalConfig, orderParams.quoteTokenMint);
    let baseTokenVault = await (0, utils_1.getTokenVaultPDA)(programAddress, globalConfig, orderParams.baseTokenMint);
    let pdaAuthority = await (0, utils_1.getPdaAuthority)(programAddress, globalConfig);
    let accounts = {
        maker,
        globalConfig,
        pdaAuthority,
        order,
        inputMint: orderParams.side === "bid"
            ? orderParams.baseTokenMint
            : orderParams.quoteTokenMint,
        outputMint: orderParams.side === "bid"
            ? orderParams.quoteTokenMint
            : orderParams.baseTokenMint,
        makerAta: orderParams.side === "bid"
            ? await (0, utils_1.getAssociatedTokenAddress)(maker.address, orderParams.baseTokenMint, baseTokenMintProgramId)
            : await (0, utils_1.getAssociatedTokenAddress)(maker.address, orderParams.quoteTokenMint, quoteTokenMintProgramId),
        inputVault: orderParams.side === "bid" ? baseTokenVault : quoteTokenVault,
        inputTokenProgram: orderParams.side === "bid"
            ? baseTokenMintProgramId
            : quoteTokenMintProgramId,
        outputTokenProgram: orderParams.side === "bid"
            ? quoteTokenMintProgramId
            : baseTokenMintProgramId,
        eventAuthority: await (0, utils_1.getEventAuthorityPDA)(programAddress),
        program: programAddress,
        systemProgram: system_1.SYSTEM_PROGRAM_ADDRESS,
    };
    let args = {
        inputAmount: new bn_js_1.default(orderParams.side === "bid"
            ? orderParams.baseTokenAmount
            : orderParams.quoteTokenAmount),
        outputAmount: new bn_js_1.default(orderParams.side === "bid"
            ? orderParams.quoteTokenAmount
            : orderParams.baseTokenAmount),
        orderType: orderParams.side === "bid" ? 0 : 1,
    };
    let ix = Instructions.createOrder(args, accounts, [], programAddress);
    return ix;
}
function updateOrder(mode, value, maker, globalConfig, order, programAddress) {
    let args = {
        mode: mode.discriminator,
        value: encodedUpdateOrderValue(mode, value),
    };
    let accounts = {
        maker,
        globalConfig,
        order,
    };
    let ix = Instructions.updateOrder(args, accounts, [], programAddress);
    return ix;
}
async function takeOrder(params) {
    let pdaAuthority = await (0, utils_1.getPdaAuthority)(params.programAddress, params.globalConfig);
    let inputVault = await (0, utils_1.getTokenVaultPDA)(params.programAddress, params.globalConfig, params.inputMint);
    let accounts = {
        taker: params.taker,
        maker: params.maker,
        globalConfig: params.globalConfig,
        pdaAuthority,
        order: params.order,
        inputMint: params.inputMint,
        outputMint: params.outputMint,
        inputVault,
        expressRelay: params.expressRelayProgramId,
        expressRelayMetadata: await (0, utils_1.getExpressRelayMetadataPDA)(params.expressRelayProgramId),
        permission: (0, utils_1.asOption)(params.permissionless ? undefined : params.order),
        configRouter: await (0, utils_1.getExpressRelayConfigRouterPDA)(params.expressRelayProgramId, pdaAuthority),
        sysvarInstructions: sysvars_1.SYSVAR_INSTRUCTIONS_ADDRESS,
        takerInputAta: params.takerInputAta,
        intermediaryOutputTokenAccount: (0, utils_1.asOption)(params.intermediaryOutputTokenAccount),
        takerOutputAta: params.takerOutputAta,
        makerOutputAta: (0, utils_1.asOption)(params.makerOutputAta),
        inputTokenProgram: params.inputTokenProgram,
        outputTokenProgram: params.outputTokenProgram,
        systemProgram: system_1.SYSTEM_PROGRAM_ADDRESS,
        rent: sysvars_1.SYSVAR_RENT_ADDRESS,
        eventAuthority: await (0, utils_1.getEventAuthorityPDA)(params.programAddress),
        program: params.programAddress,
    };
    let args = {
        inputAmount: params.inputAmountLamports,
        minOutputAmount: params.minOutputAmountLamports,
        tipAmountPermissionlessTaking: params.permissionlessTipLamports,
    };
    let ix = Instructions.takeOrder(args, accounts, [], params.programAddress);
    return ix;
}
async function flashTakeOrder(params) {
    let pdaAuthority = await (0, utils_1.getPdaAuthority)(params.programAddress, params.globalConfig);
    let inputVault = await (0, utils_1.getTokenVaultPDA)(params.programAddress, params.globalConfig, params.inputMint);
    let startAccounts = {
        taker: params.taker,
        maker: params.maker,
        globalConfig: params.globalConfig,
        pdaAuthority,
        order: params.order,
        inputMint: params.inputMint,
        outputMint: params.outputMint,
        inputVault,
        expressRelay: params.expressRelayProgramId,
        expressRelayMetadata: await (0, utils_1.getExpressRelayMetadataPDA)(params.expressRelayProgramId),
        permission: (0, utils_1.asOption)(params.permissionless ? undefined : params.order),
        configRouter: await (0, utils_1.getExpressRelayConfigRouterPDA)(params.expressRelayProgramId, pdaAuthority),
        sysvarInstructions: sysvars_1.SYSVAR_INSTRUCTIONS_ADDRESS,
        takerInputAta: params.takerInputAta,
        takerOutputAta: params.takerOutputAta,
        intermediaryOutputTokenAccount: (0, utils_1.asOption)(params.intermediaryOutputTokenAccount),
        makerOutputAta: (0, utils_1.asOption)(params.makerOutputAta),
        inputTokenProgram: params.inputTokenProgram,
        outputTokenProgram: params.outputTokenProgram,
        systemProgram: system_1.SYSTEM_PROGRAM_ADDRESS,
        rent: sysvars_1.SYSVAR_RENT_ADDRESS,
        eventAuthority: await (0, utils_1.getEventAuthorityPDA)(params.programAddress),
        program: params.programAddress,
    };
    let startArgs = {
        inputAmount: new bn_js_1.default(params.inputAmountLamports),
        minOutputAmount: new bn_js_1.default(params.minOutputAmountLamports),
        tipAmountPermissionlessTaking: params.permissionlessTipLamports ?? new bn_js_1.default(0),
    };
    let startIx = Instructions.flashTakeOrderStart(startArgs, startAccounts, [], params.programAddress);
    let endAccounts = {
        taker: params.taker,
        maker: params.maker,
        globalConfig: params.globalConfig,
        pdaAuthority,
        order: params.order,
        inputMint: params.inputMint,
        outputMint: params.outputMint,
        inputVault,
        expressRelay: params.expressRelayProgramId,
        expressRelayMetadata: await (0, utils_1.getExpressRelayMetadataPDA)(params.expressRelayProgramId),
        permission: (0, utils_1.asOption)(params.permissionless ? undefined : params.order),
        configRouter: await (0, utils_1.getExpressRelayConfigRouterPDA)(params.expressRelayProgramId, pdaAuthority),
        sysvarInstructions: sysvars_1.SYSVAR_INSTRUCTIONS_ADDRESS,
        takerInputAta: params.takerInputAta,
        takerOutputAta: params.takerOutputAta,
        intermediaryOutputTokenAccount: (0, utils_1.asOption)(params.intermediaryOutputTokenAccount),
        makerOutputAta: (0, utils_1.asOption)(params.makerOutputAta),
        inputTokenProgram: params.inputTokenProgram,
        outputTokenProgram: params.outputTokenProgram,
        systemProgram: system_1.SYSTEM_PROGRAM_ADDRESS,
        rent: sysvars_1.SYSVAR_RENT_ADDRESS,
        eventAuthority: await (0, utils_1.getEventAuthorityPDA)(params.programAddress),
        program: params.programAddress,
    };
    let endArgs = {
        inputAmount: new bn_js_1.default(params.inputAmountLamports),
        minOutputAmount: new bn_js_1.default(params.minOutputAmountLamports),
        tipAmountPermissionlessTaking: params.permissionlessTipLamports ?? new bn_js_1.default(0),
    };
    let endIx = Instructions.flashTakeOrderEnd(endArgs, endAccounts, [], params.programAddress);
    return {
        startIx,
        endIx,
    };
}
async function closeOrderAndClaimTip(params) {
    let pdaAuthority = await (0, utils_1.getPdaAuthority)(params.programAddress, params.globalConfig);
    let inputVault = await (0, utils_1.getTokenVaultPDA)(params.programAddress, params.globalConfig, params.inputMint);
    let accounts = {
        maker: params.maker,
        order: params.order,
        globalConfig: params.globalConfig,
        pdaAuthority,
        inputMint: params.inputMint,
        outputMint: params.outputMint,
        makerInputAta: params.makerInputAta,
        inputVault,
        inputTokenProgram: params.inputTokenProgram,
        systemProgram: system_1.SYSTEM_PROGRAM_ADDRESS,
        eventAuthority: await (0, utils_1.getEventAuthorityPDA)(params.programAddress),
        program: params.programAddress,
    };
    let ix = Instructions.closeOrderAndClaimTip(accounts, [], params.programAddress);
    return ix;
}
async function withdrawHostTipIx(params) {
    let pdaAuthority = await (0, utils_1.getPdaAuthority)(params.programAddress, params.globalConfig);
    let accounts = {
        adminAuthority: params.admin,
        globalConfig: params.globalConfig,
        pdaAuthority,
        systemProgram: system_1.SYSTEM_PROGRAM_ADDRESS,
    };
    let ix = Instructions.withdrawHostTip(accounts, [], params.programAddress);
    return ix;
}
function updateGlobalConfigIx(admin, globalConfig, mode, value, programAddress) {
    let accounts = {
        adminAuthority: admin,
        globalConfig,
    };
    let args = {
        mode: mode.discriminator,
        value: encodedUpdateGlobalConfigValue(mode, value),
    };
    let ix = Instructions.updateGlobalConfig(args, accounts, [], programAddress);
    return ix;
}
function updateGlobalConfigAdminIx(admin, globalConfigAddress, globalConfig, programAddress) {
    let accounts = {
        // TODO do we really need a cached admin authority? If yes, we need to cache the keypair not just the address
        //adminAuthorityCached: createNoopSigner(globalConfig.adminAuthorityCached),
        adminAuthorityCached: admin,
        globalConfig: globalConfigAddress,
    };
    let ix = Instructions.updateGlobalConfigAdmin(accounts, [], programAddress);
    return ix;
}
function encodedUpdateOrderValue(mode, value) {
    let valueBoolean;
    let valuePublicKey;
    let valueData;
    switch (mode.discriminator) {
        case index_1.UpdateOrderMode.UpdatePermissionless.discriminator:
            valueBoolean = value ? 1 : 0;
            valueData = Buffer.alloc(1);
            valueData.writeUIntLE(valueBoolean, 0, 1);
            break;
        case index_1.UpdateOrderMode.UpdateCounterparty.discriminator:
            valuePublicKey = Buffer.from((0, kit_1.getAddressEncoder)().encode(value));
            valueData = Buffer.alloc(32);
            valuePublicKey.copy(valueData, 0);
            break;
    }
    const uint8Array = new Uint8Array(valueData);
    return uint8Array;
}
function encodedUpdateGlobalConfigValue(mode, value) {
    const valueData = Buffer.alloc(128);
    let valueNumber;
    let valueAddress;
    switch (mode.discriminator) {
        case index_1.UpdateGlobalConfigMode.UpdateEmergencyMode.discriminator:
        case index_1.UpdateGlobalConfigMode.UpdateFlashTakeOrderBlocked.discriminator:
        case index_1.UpdateGlobalConfigMode.UpdateBlockNewOrders.discriminator:
        case index_1.UpdateGlobalConfigMode.UpdateBlockOrderTaking.discriminator:
        case index_1.UpdateGlobalConfigMode.UpdateOrderTakingPermissionless.discriminator:
            valueNumber = value;
            valueData.writeUIntLE(valueNumber, 0, 1);
            break;
        case index_1.UpdateGlobalConfigMode.UpdateHostFeeBps.discriminator:
            valueNumber = value;
            valueData.writeUInt16LE(valueNumber, 0);
            break;
        case index_1.UpdateGlobalConfigMode.UpdateOrderCloseDelaySeconds.discriminator:
        case index_1.UpdateGlobalConfigMode.UpdateAtaCreationCost.discriminator:
        case index_1.UpdateGlobalConfigMode.UpdateTxnFeeCost.discriminator:
        case index_1.UpdateGlobalConfigMode.UpdateOrderCloseDelaySeconds.discriminator:
            valueNumber = value;
            valueData.writeBigUInt64LE(BigInt(value.toString()), 0);
            break;
        case index_1.UpdateGlobalConfigMode.UpdateAdminAuthorityCached.discriminator:
            valueAddress = Buffer.from((0, kit_1.getAddressEncoder)().encode(value));
            valueAddress.copy(valueData, 0);
            break;
    }
    const uint8Array = new Uint8Array(valueData);
    return Array.from(uint8Array);
}
//# sourceMappingURL=operations.js.map