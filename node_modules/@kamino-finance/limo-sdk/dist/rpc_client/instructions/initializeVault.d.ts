import { Address, AccountMeta, AccountSignerMeta, Instruction, TransactionSigner } from "@solana/kit";
export declare const DISCRIMINATOR: Buffer;
export interface InitializeVaultAccounts {
    payer: TransactionSigner;
    globalConfig: Address;
    pdaAuthority: Address;
    mint: Address;
    vault: Address;
    tokenProgram: Address;
    systemProgram: Address;
}
export declare function initializeVault(accounts: InitializeVaultAccounts, remainingAccounts?: Array<AccountMeta | AccountSignerMeta>, programAddress?: Address): Instruction<string, readonly (AccountMeta<string> | import("@solana/kit").AccountLookupMeta<string, string>)[]>;
//# sourceMappingURL=initializeVault.d.ts.map