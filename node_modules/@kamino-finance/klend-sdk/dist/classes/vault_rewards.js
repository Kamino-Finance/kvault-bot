"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.buildTopupVaultRewardsIxs = buildTopupVaultRewardsIxs;
exports.buildWithdrawVaultRewardsIxs = buildWithdrawVaultRewardsIxs;
exports.calculateVaultRewardsAprApy = calculateVaultRewardsAprApy;
const bn_js_1 = __importDefault(require("bn.js"));
const decimal_js_1 = __importDefault(require("decimal.js"));
const kit_1 = require("@solana/kit");
const token_1 = require("@solana-program/token");
const token_2022_1 = require("@solana-program/token-2022");
const instructions_1 = require("../@codegen/kvault/instructions");
const ata_1 = require("../utils/ata");
const constants_1 = require("../utils/constants");
const consts_1 = require("../utils/consts");
const signer_1 = require("../utils/signer");
const utils_1 = require("./utils");
async function prepareVaultRewardsTransfer(owner, vaultState, tokenAmount, wrapWsol) {
    const tokenMintDecimals = vaultState.tokenMintDecimals.toNumber();
    const tokenAmountLamports = (0, utils_1.numberToLamportsDecimal)(tokenAmount, tokenMintDecimals).floor();
    if (tokenAmount.gt(0) && tokenAmountLamports.lte(0)) {
        throw new Error(`Reward amount ${tokenAmount.toFixed()} rounds to 0 lamports at ${tokenMintDecimals} decimals`);
    }
    const [{ ata: tokenAccount, createAtaIx }] = await (0, ata_1.createAtasIdempotent)(owner, [
        {
            mint: vaultState.tokenMint,
            tokenProgram: vaultState.tokenProgram,
        },
    ]);
    const prerequisiteIxs = [createAtaIx];
    const cleanupIxs = [];
    if (vaultState.tokenMint === consts_1.WRAPPED_SOL_MINT) {
        if (wrapWsol) {
            prerequisiteIxs.push(...(0, ata_1.getTransferWsolIxs)(owner, tokenAccount, (0, kit_1.lamports)(BigInt(tokenAmountLamports.toString())), vaultState.tokenProgram));
        }
        cleanupIxs.push((0, token_2022_1.getCloseAccountInstruction)({
            account: tokenAccount,
            owner,
            destination: owner.address,
        }, { programAddress: token_1.TOKEN_PROGRAM_ADDRESS }));
    }
    return {
        tokenAccount,
        tokenAmountLamports,
        prerequisiteIxs,
        cleanupIxs,
    };
}
async function buildTopupVaultRewardsIxs(kaminoVaultProgramId, payer, vault, tokenAmount) {
    const vaultState = await vault.getState();
    const { tokenAccount, tokenAmountLamports, prerequisiteIxs, cleanupIxs } = await prepareVaultRewardsTransfer(payer, vaultState, tokenAmount, true);
    const accounts = {
        payer,
        vaultState: vault.address,
        tokenMint: vaultState.tokenMint,
        tokenVault: vaultState.tokenVault,
        payerTokenTa: tokenAccount,
        tokenProgram: vaultState.tokenProgram,
    };
    const args = {
        amount: new bn_js_1.default(tokenAmountLamports.toString()),
    };
    return {
        prerequisiteIxs,
        topupIxs: [(0, instructions_1.topupRewards)(args, accounts, undefined, kaminoVaultProgramId)],
        cleanupIxs,
    };
}
async function buildWithdrawVaultRewardsIxs(kaminoVaultProgramId, vault, tokenAmount, vaultAdminAuthority) {
    const vaultState = await vault.getState();
    const vaultAdmin = vaultAdminAuthority ?? (0, signer_1.noopSigner)(vaultState.vaultAdminAuthority);
    const { tokenAccount, tokenAmountLamports, prerequisiteIxs, cleanupIxs } = await prepareVaultRewardsTransfer(vaultAdmin, vaultState, tokenAmount, false);
    const accounts = {
        vaultAdminAuthority: vaultAdmin,
        vaultState: vault.address,
        tokenMint: vaultState.tokenMint,
        tokenVault: vaultState.tokenVault,
        baseVaultAuthority: vaultState.baseVaultAuthority,
        withdrawTokenAccount: tokenAccount,
        tokenProgram: vaultState.tokenProgram,
    };
    const args = {
        amount: new bn_js_1.default(tokenAmountLamports.toString()),
    };
    return {
        prerequisiteIxs,
        withdrawIxs: [(0, instructions_1.withdrawRewards)(args, accounts, undefined, kaminoVaultProgramId)],
        cleanupIxs,
    };
}
/** Calculate the fixed-emission reward APR/APY from a token-lamport rate and token-denominated net AUM. */
function calculateVaultRewardsAprApy(rewardPerSecondLamports, tokenMintDecimals, netAumTokens) {
    if (rewardPerSecondLamports.lte(0) || netAumTokens.lte(0)) {
        return {
            apr: new decimal_js_1.default(0),
            apy: new decimal_js_1.default(0),
        };
    }
    const rewardsPerYearTokens = rewardPerSecondLamports
        .mul(constants_1.SECONDS_PER_YEAR)
        .div(new decimal_js_1.default(10).pow(tokenMintDecimals));
    const apr = rewardsPerYearTokens.div(netAumTokens);
    return {
        apr,
        // The program emits a fixed token amount per second rather than maintaining a percentage rate
        // as AUM grows, so the reward-only annual yield is linear and does not compound.
        apy: apr,
    };
}
//# sourceMappingURL=vault_rewards.js.map