import { Address, Rpc, GetMultipleAccountsApi } from '@solana/kit';
import Decimal from 'decimal.js';
import type { LedgerInstant } from './ledger';
/**
 * Batch-fetches mint accounts for the given addresses and returns a map of
 * mint address → scaledUiAmount multiplier. Only Token-2022 mints with
 * the ScaledUiAmountConfig extension will have multiplier !== 1.
 *
 * Mints that fail to fetch are silently skipped (multiplier defaults to 1).
 */
export declare function fetchScaledUiAmountMultipliers(rpc: Rpc<GetMultipleAccountsApi>, mintAddresses: Address[], currentLedgerInstant: LedgerInstant): Promise<Map<Address, Decimal>>;
//# sourceMappingURL=scaledUiAmount.d.ts.map