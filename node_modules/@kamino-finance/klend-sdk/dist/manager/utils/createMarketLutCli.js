"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.assertCreateMarketLutCliOptions = assertCreateMarketLutCliOptions;
exports.resolveCreateMarketLutTxSigner = resolveCreateMarketLutTxSigner;
const signer_1 = require("../../utils/signer");
/**
 * Validate create-market-lut CLI option combinations before env/RPC setup.
 * Multisig mode may only extend an existing LUT (createLookupTable recentSlot expires before approval)
 * and always uses --multisig as authority/payer (not --signer / ADMIN).
 */
function assertCreateMarketLutCliOptions(opts) {
    if (opts.mode !== 'multisig') {
        return;
    }
    if (!opts.multisig) {
        throw new Error('If using multisig mode, multisig is required');
    }
    if (!opts.existingLut) {
        throw new Error('mode=multisig requires --existing-lut for an existing LUT already controlled by --multisig: createLookupTable recentSlot can expire before multisig approval, and a LUT created with --mode execute can have a different authority');
    }
    if (opts.signer) {
        throw new Error('--signer cannot be used with --mode multisig; --multisig selects the LUT authority/payer');
    }
}
/**
 * Resolve the LUT authority/payer for create-market-lut.
 * In multisig mode this is always the --multisig address (noop signer).
 */
function resolveCreateMarketLutTxSigner(mode, multisig, signerOverride, defaultSigner) {
    switch (mode) {
        case 'multisig':
            if (!multisig) {
                throw new Error('If using multisig mode, multisig is required');
            }
            return (0, signer_1.noopSigner)(multisig);
        case 'simulate':
        case 'execute':
        case 'print':
            if (signerOverride) {
                return signerOverride;
            }
            if (!defaultSigner) {
                throw new Error('No signer available for create-market-lut');
            }
            return defaultSigner;
        default: {
            const _exhaustive = mode;
            throw new Error(`Unhandled mode: ${_exhaustive}`);
        }
    }
}
//# sourceMappingURL=createMarketLutCli.js.map